# Python
__pycache__/
*.pyc
*.db

# Secrets / env files (绝对禁止提交，详见 CLAUDE.md「机密保护规则」)
.env
.env.*
!.env.example
review_analyzer/.env
backend_api/.env
deploy/.env
deploy/.env.*
!deploy/.env.example
frontend/.env
frontend/.env.local
frontend/.env.production
.streamlit/secrets.toml

# macOS
.DS_Store

# Build artifacts
frontend/.next/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo

# Chrome extension build output (regenerated by scripts/build_extension_zip.sh)
/dist/

# Taxonomy 中间产物 (yaml 与 summary.md 进 git, 其余只在本地)
data/processed/
data/taxonomy/v1.0/*/aspect_extraction_raw.jsonl
data/taxonomy/v1.0/*/extraction.log
data/taxonomy/v1.0/REVIEW_SHEET.xlsx
data/taxonomy/v1.0/.~*.xlsx
data/taxonomy/v1.0/backup-*/

# 本地文档 / 探索产物（永不进 git，详见 CLAUDE.md「本地文档与目录约定」）
/notes/
/scratch/
/.playwright-mcp/
/node_modules/
/*.png
/*.jpg
/*.jpeg

# 项目进度文档（仅本地留存，详见 CODEX.md「PROGRESS_V2.md 保护规则」）
PROGRESS_V2.md
PROGRESS_V2.md.backup

# Golden set / raw data（不进 git，本地标注数据）
data/golden_set/
data/raw/

# Test artifacts（不进 git）
test-results/
test_data/

# Python 构建产物
*.egg-info/

# 根目录截图（Playwright MCP 产物已归位到 .playwright-mcp/，此处兜底）
/screenshots/

# Chrome 扩展商店素材（不进 git，仅本地留存）
chrome-extension/promotional/
chrome-extension/screenshots/
chrome-extension/tests/STEP14-3-E2E-REPORT.md

# 根目录误放的 npm 文件
/package.json
/package-lock.json

# 一次性导入脚本（不进 git）
data/taxonomy/import_taxonomy_to_db.py

# review_analyzer 本地数据文件
review_analyzer/data/

# 误放的 SSH 别名文件
/ubuntu@13.215.29.99
