# 5.9.9 Step 8: Vector bad case memory lite

日期：2026-07-31

状态：`PASS`

## 1. 结论

已新增 Customer Label v2 lightweight/local bad case memory，用于 verifier/debug/audit 辅助。

本轮只做本地 sparse token vector memory、focused tests、artifact 和文档：

- 不接生产写库。
- 不开启生产 feature flag。
- 不改变当前 frontstage 默认行为。
- 不触发 production upload。
- 不触发 production reanalysis。
- 不写 production DB。
- 不消耗 credit。
- 不调用真实 LLM。
- 不 push。

Vector memory 只能辅助：

- 相似历史错例检索
- candidate 聚类
- 审核排序
- audit/debug report

Vector result 不直接决定前台展示；display 仍必须由 evidence gate、context gate、aspect gate、maturity gate、feature flag/read-path gate 决定。

核心新增：

- `backend_api/app/services/customer_label_v2_bad_case_memory.py`
- `backend_api/tests/test_customer_label_v2_bad_case_memory.py`
- `tmp/5.9.9-step8-vector-bad-case-memory-lite/vector-bad-case-memory-lite.json`

## 2. Memory Scope

本地 memory 输入：

| Source | Count |
|---|---:|
| audited bad case | `32` |
| candidate pool reviewed | `14` |
| gold regression | `35` |
| total | `81` |

主要 downgrade reasons：

- `maturity_blocked=16`
- `unknown_label=12`
- `evidence_too_generic=5`
- `source_review_blocked=2`
- `context_blocked=2`
- `aspect_blocked=2`
- `evidence_not_found=2`
- `evidence_missing=1`

## 3. Debug Outputs

Artifact 包含：

- `similar_history`: 3 个本地 query 的相似 bad case 检索结果
- `unknown_candidate_clusters`: unknown/candidate label 聚类，当前 cluster count `1`
- `maturity_blocked_priorities`: maturity blocked candidate 审核排序，当前 priority count `10`
- `display_gate_probe`: no evidence / context blocked 不进 frontstage 的验证

Display gate probe：

| Probe | Display count | Reasons |
|---|---:|---|
| no evidence | `0` | `evidence_missing`, `evidence_too_generic` |
| context blocked | `0` | `source_review_blocked` |

## 4. Artifact

生成：

- `tmp/5.9.9-step8-vector-bad-case-memory-lite/vector-bad-case-memory-lite.json`

Artifact safety flags:

- `production_upload=false`
- `production_write_path=false`
- `production_db_write=false`
- `credit_consumed=false`
- `llm_called=false`
- `frontstage_replaced=false`
- `vector_result_decides_display=false`

## 5. Verification

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_bad_case_memory.py -q
```

Result: `5 passed in 0.08s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_bad_case_memory.py backend_api/tests/test_customer_label_v2_shadow.py backend_api/tests/test_customer_label_v2_frontstage.py -q
```

Result: `32 passed in 0.58s`

```bash
python3 scripts/customer_label_v2_waders_shadow_replay.py
```

Result: `PASS`, P0=`0`

## 6. Remaining Risk

- 当前是 lightweight/local sparse vector，不是生产向量库，也没有 pgvector/embedding 写入。
- Memory 的召回质量只能作为 debug/audit 辅助；不能替代人工审核、gold regression 或 display gates。
- 后续如接生产向量库，必须单独评审隐私、成本、写路径、数据保留和回滚。
