# 5.9.8 Step 4: TIDEWE waders session122 生产完整验收

日期：2026-07-30

> 勘误：session121 之前曾被误以为已部署上线，后续确认 CI/CD 未成功把新构建推到生产，因此 session121 的 production download 结论作废。本报告记录 session122 的真正部署后完整验收。

## 1. 结论

`REVIEW_NEEDED`。

session122 的下载结构修复已上线，但完整标签语义验收未通过，因此不能把 `PROGRESS_V2` 中 Step 4 改为 `PASS`，也不能进入 `5.9.9 Customer Label System v2 Step 1`。

本轮四条路径结论：

| 路径 | 结果 | 说明 |
|---|---|---|
| Top10 | `REVIEW_NEEDED` | 本地 post-fix 只读回放无 Top/evidence/cluster P0；但当前生产下载暴露 `water_leaks_through` 语义 P0，需部署后重新验收结果页 Top10。 |
| 单条评论明细 | `REVIEW_NEEDED` | 当前生产下载中发现 2 个前台语义 P0：旧/他牌漏水污染 1、当前产品漏水漏召回 1。 |
| single-tag download | `PASS`（本地 post-fix 等价文件） | 当前桌面未发现 Erika 实际下载的 single-tag 文件；基于 session122 生产只读数据和前端同口径逻辑生成的 6 个 single-tag 文件均为 PASS。 |
| raw review download | `REVIEW_NEEDED` | Raw/Audit 分列结构 PASS，但 Raw Reviews 前台字段仍有 `water_leaks_through` 语义 P0。 |

## 2. 生产下载结构审计

文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-AGGREGATED-301-350.xlsx`

| 项目 | 结果 |
|---|---|
| sheets | `Raw Reviews`, `Label Audit`, `Export Info` |
| Raw Reviews rows / cols | `50 / 15` |
| Label Audit rows / cols | `50 / 30` |
| `frontstage_verified_violations` | `0` |
| audit issue evidence false / cluster true | `13 / 13` |
| audit highlight evidence false / cluster true | `17 / 17` |

结构结论：

- `Raw Reviews` 已不再混入 audit flag 列。
- `Label Audit` 已提供 frontstage 与 audit 分列。
- evidence false / cluster propagated 候选保留在 `Label Audit`，未作为 flag 列进入 raw 前台字段。

## 3. P0 详情

完整审计 artifact：

- `tmp/5.9.8-step4-tidewe-waders-prod-20260729/session122-readonly/session122-full-acceptance-audit.json`
- `tmp/5.9.8-step4-tidewe-waders-prod-20260729/session122-readonly/download-file-audit.json`
- `tmp/5.9.8-step4-tidewe-waders-prod-20260729/session122-readonly/acceptance-summary.json`

生产 raw 下载 P0：

| row | key | 类型 | 当前生产前台输出 |
|---:|---|---|---|
| 9 | `water_leaks_through` | 旧产品/他牌漏水污染 | review 提到 `other neoprene waders ... leak`，但当前 TIDEWE waders 同条 evidence 是 `keep you dry`；生产前台仍输出 `Water Leaks Through`。 |
| 38 | `water_leaks_through` | 当前产品漏水漏召回 | review 为 `They do not keep you dry.`；生产前台未输出 `Water Leaks Through`。 |

这两个问题都落在本轮指定重点标签 `water_leaks_through` 上，因此 P0 不为 0。

## 4. 本地修复与回归

已做本地窄修复：

- 后端 `water_leaks_through` 召回支持 `do/does not keep ... dry` 与 `don't/doesn't keep ... dry` 语境。
- 后端与前端同步阻断 `other + 描述词 + waders + leak`，覆盖 `other neoprene waders that continue to leak`。
- 盲测 fixture 新增 session122 两个回归样例。
- 只读验收脚本同步相同启发式，避免把已正确阻断的旧产品漏水误报为 FN。

本地 post-fix session122 只读回放：

| 项目 | 结果 |
|---|---|
| validation.status | `PASS` |
| Top violations | `0` |
| single-detail no evidence / cluster / evidence-not-found | `0 / 0 / 0` |
| water FP / FN candidates | `0 / 0` |
| pocket FN candidates | `0` |
| six single-tag equivalent exports | `PASS` |

六个 single-tag 等价下载：

| key | rows | 结果 |
|---|---:|---|
| `water_leaks_through` | 5 | `PASS` |
| `pocket_not_waterproof` | 0 | `PASS` |
| `keeps_water_out` | 4 | `PASS` |
| `fits_as_expected` | 6 | `PASS` |
| `good_value_for_the_price` | 9 | `PASS` |
| `holds_up_well` | 2 | `PASS` |

## 5. 自动化验证

已执行：

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session121_blind_regression.py -q
```

结果：PASS，`2 passed in 0.50s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_step4_gold.py -q
```

结果：PASS，`3 passed in 1.65s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session121_blind_regression.py backend_api/tests/test_customer_label_waders_session120_human_gold.py backend_api/tests/test_customer_label_waders_step4_gold.py backend_api/tests/test_customer_label_waders_step2_gating.py backend_api/tests/test_specific_issue.py backend_api/tests/test_customer_label_phase6_validation.py backend_api/tests/test_export_customer_label_phase5.py backend_api/tests/test_customer_label_50u_readiness.py backend_api/tests/test_outdoor_waders_taxonomy.py -q
```

结果：PASS，`82 passed in 6.10s`

```bash
npm run typecheck
```

cwd：`frontend`

结果：PASS，`tsc --noEmit`

```bash
git diff --check
```

结果：PASS

## 6. 安全边界

Codex 本轮执行：

- 生产 DB 只读查询。
- 读取 Erika 已下载的 production XLSX。
- 本地 artifact 生成与本地规则修复。
- 本地 pytest。

Codex 未执行：

- 未执行生产上传。
- 未删除生产数据。
- 未清理 production `review_pool`。
- 未触发 production reanalysis。
- 未触发 production LLM 或 credit。
- 未调用 production export API 生成新下载。

## 7. 下一步

1. 部署本轮窄修复。
2. Erika 从前端重新下载 session122 或上传新的干净 50 条 waders 样本。
3. 重新核验 Top10、单条评论明细、raw review download、single-tag download 四条路径。
4. 只有当生产实测 P0=0 时，才把 `PROGRESS_V2` 的 Step 4 改为 `PASS` 并进入 `5.9.9 Customer Label System v2 Step 1`。
