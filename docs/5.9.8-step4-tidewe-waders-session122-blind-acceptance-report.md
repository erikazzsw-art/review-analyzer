# 5.9.8 Step 4: TIDEWE waders session122 生产下载纠正验收

日期：2026-07-30

> 勘误：session121 之前曾被误以为已部署上线，后续确认 CI/CD 未成功把新构建推到生产，因此 session121 的 production download 结论作废。本报告记录 session122 的真正部署后下载纠正。

## 1. 结论

`PASS`（限生产下载导出口径）。

session122 的下载文件 `TIDEWE-下水服-WD001-AGGREGATED-301-350.xlsx` 已把前台字段与 audit 字段正确分离：

- `Raw Reviews` 保持 15 列前台字段。
- `Label Audit` 提供 30 列 audit / frontstage 分列。
- `download-file-audit.json` 的 `status` 为 `PASS`。
- `frontstage_verified_violations` 为空。

## 2. 审计摘要

文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-AGGREGATED-301-350.xlsx`

| 项目 | 结果 |
|---|---|
| sheets | `Raw Reviews`, `Label Audit`, `Export Info` |
| raw rows / cols | `50 / 15` |
| label audit rows / cols | `50 / 30` |
| frontstage verified violations | `0` |
| duplicate frontstage rows | `8` |
| audit issue evidence false / cluster true | `13 / 13` |
| audit highlight evidence false / cluster true | `17 / 17` |

结论：

- frontstage 只保留 verified source-review occurrence。
- candidate / propagated / evidence false 继续只出现在 `Label Audit`。
- `duplicate_frontstage_rows` 反映的是同一评论内的重复 canonical key 计数，不是 audit 污染。
- 这次纠正替换了 session121 的失效生产结论。

## 3. 只读范围

- 仅审计下载文件与其导出结构。
- 未执行生产上传、删除、重分析或 LLM 调用。
