# 5.9.8 Step 4.1: TIDEWE waders session120 人工 gold 召回修复

日期：2026-07-29

## 1. 结论

本轮只做本地修复、fixture/gold set、自动化测试和文档更新；未执行生产上传、未删除生产数据、未清理 production review_pool、未触发 production LLM / credit / reanalysis。

session120 当前生产下载文件仍不能判 PASS，因为它仍是旧 raw/audit 口径。本轮的 PASS 结论仅针对本地最新代码 replay 和自动化 gold 测试。

本地最新 session120 human gold 结果：

| 指标 | 结果 |
|------|------|
| both exact set | `50/50` |
| issue exact set | `50/50` |
| highlight exact set | `50/50` |
| issue TP / FP / FN | `22 / 0 / 0` |
| highlight TP / FP / FN | `49 / 0 / 0` |

对比修复前本地 replay：both exact `10/50`、issue exact `37/50`、highlight exact `18/50`。

## 2. 输入与 Fixture

输入文件：

- `/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-session_id=120.xlsx`
- `docs/5.9.8-step4-tidewe-waders-session120-human-label-comparison.md`
- `docs/5.9.8-step4-tidewe-waders-session120-acceptance-report.md`

新增 fixture：

- `backend_api/tests/fixtures/customer_label_waders_session120_human_gold.json`

fixture 覆盖 50 条评论，每条包含：

- 人工 issue/highlight canonical keys
- 本地当前 frontstage output
- false positive / false negative
- wrong aspect / wrong evidence

口径说明：

- `not_used_yet` 仅作为 context/audit，不进入用户前台 Customer Label。
- 人工亮点列中误填的明显问题项，例如 `尺码不准`，按 issue gold 计入，不计入 highlight gold。
- exact 使用 canonical key set 比对，不因输出顺序不同而失败。

## 3. 修复范围

Issue 召回补齐：

- `size_fit_problem`
- `strong_chemical_smell`
- `breaks_easily`
- `quality_control_storage_issue`
- `quality_problem`
- `soft_soles`
- `not_breathable`

Highlight 召回补齐：

- `overall_satisfied`
- `good_value_for_the_price`
- `looks_good`
- `holds_up_well`
- `fits_as_expected`
- `keeps_water_out`
- `works_well_for_use_case`
- `useful_storage_space`

FP guard：

- `good_material_quality` 不再从 `appear/seem/well made` 或纯外观第一印象泛化出来；明确 `Good quality` / `Great quality` 仍可召回。
- `works_well_for_use_case` 需要明确使用场景成功证据，例如 `great for wade fishing`、`worked well ... brush/marsh`、`worked great ... fishing/wading/river/creek/dock/brush`；单独地点或活动名不触发。
- `not_used_yet` 只保留 audit/context，不进前台明细、Top、raw frontstage 或单标签下载。
- `cluster_propagated=true`、无原文 evidence、旧 legacy fallback 不进入前台展示。

## 4. 代码与文档变更

核心代码：

- `backend_api/app/services/specific_issue.py`
  - waders content-rule occurrence 增补 size/fit、气味、品控/质量、鞋底偏软、透气、西语透气差、整体满意、性价比、外观、耐用、防水、场景适用和储物。
  - `not_used_yet` 改为 context/audit-only。
  - 收紧 `good_material_quality`、`good_value_for_the_price`、`works_well_for_use_case`、`holds_up_well` 等上下文 guard。
- `frontend/src/lib/customer-labels.ts`
  - 同步 allowed aspect keys。
  - 前端 occurrence/aspect-derived 两条路径同步 waders context guard。
  - 修复 tag text 旁路，避免 context-disallowed occurrence 被旧 aspects fallback 重新带回前台。
- `backend_api/app/prompts/annotate_v2.4.md`
  - 补充 waders size/fit、未使用、外观/材质、use-case 成功证据边界。
- `data/taxonomy/v1.0/outdoor/waders.yaml`
  - 补充 size_fit、boot_fit、mobility、material、build_quality、accessory_storage、value_for_money、other 边界说明。

测试：

- `backend_api/tests/test_customer_label_waders_session120_human_gold.py`
- `backend_api/tests/fixtures/customer_label_waders_step2_gating.json`
- `backend_api/tests/fixtures/customer_label_waders_step4_gold.json`
- `backend_api/tests/test_customer_label_waders_step2_gating.py`

## 5. 自动化验证

已执行：

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session120_human_gold.py -q
```

结果：PASS，`7 passed in 3.87s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session120_human_gold.py backend_api/tests/test_customer_label_waders_step4_gold.py backend_api/tests/test_customer_label_waders_step2_gating.py backend_api/tests/test_specific_issue.py backend_api/tests/test_customer_label_phase6_validation.py backend_api/tests/test_export_customer_label_phase5.py backend_api/tests/test_customer_label_50u_readiness.py backend_api/tests/test_outdoor_waders_taxonomy.py -q
```

结果：PASS，`80 passed in 11.52s`

```bash
npm run typecheck
```

cwd：`frontend`

结果：PASS，`tsc --noEmit`

```bash
git diff --check
```

结果：PASS

## 6. Erika 手动上传验收清单

前置：

- 使用部署了本轮代码的新环境。
- 不用 session120 旧下载文件作为 PASS 依据。
- 用前端手动上传新的 TIDEWE waders 50 条或同等干净样本。
- 上传会触发真实分析、credit 和 LLM 成本，需 Erika 手动执行。

验收项：

1. 结果页单条评论明细只显示当前评论原文可定位的 Customer Issue / Customer Label。
2. raw review 下载中 `Canonical Issue Key` / `Canonical Highlight Key` 只包含 frontstage 标签。
3. raw review 下载中 `Audit Canonical ...` 可保留 candidate、not_used_yet、cluster propagated、evidence false，但字段必须明确在 Audit 列。
4. 单标签下载的评论集合与结果页 Top row 的 canonical key 一致。
5. `cluster_propagated=true` 不出现在前台标签、Top mention、single-tag 下载。
6. `not_used_yet` 不出现在用户前台标签、Top 或 single-tag 下载。
7. `good_material_quality` 不由单纯 `looks good` / `appear well made` 泛化。
8. `works_well_for_use_case` 不由单独地点/活动名推断。
9. `water_leaks_through` 仍需满足人工 3 条命中 3 条，FP 0。
10. session120 human gold 重点类别在新上传结果中无明显系统性漏召回。

PASS 标准：

- 前台 both exact 或人工抽查等价结果达到可用阈值：明显错误标签 `<= 1`。
- `water_leaks_through` 人工 3 / 命中 3 / FP 0。
- `not_used_yet`、cluster propagated、evidence false 不进入前台。
- raw/frontstage/Top/single-tag 下载口径一致。

FAIL 标准：

- session120 旧生产下载被直接拿来判 PASS。
- 前台仍批量出现四件套无 evidence 扩散。
- `not_used_yet` 进入用户展示标签或 Top。
- `works_well_for_use_case` 由无证据地点/活动推断。
- `good_material_quality` 被所有“看起来不错”泛化。
- raw 前台字段继续混入 audit/candidate/cluster propagated。
