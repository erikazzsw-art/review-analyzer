# 5.9.9 Step 9.1: Review Signal Layer Minimal Shadow Validation

日期：2026-07-31

状态：`PHASE4_READY_FOR_IMPLEMENTATION_AUTHORIZATION_RUNTIME_UNCHANGED`

## 1. 结论

已完成 Step 9.1 Phase 1-3 的本地 shadow 验证版：新增片段级 `review_signals` schema / helper、AirPods 长评核心片段 fixture、focused tests、本地 gold assimilation、signal-derived routing projection，以及 baseline vs signal shadow FP/FN 对比 artifact。

本轮只验证“片段分流能否阻止 Customer Issue / Customer Label 错收非产品信号”，不改变任何用户可见结果。当前 routing gate 通过：非产品信号进入 Customer Issue / Customer Label 的 leakage 为 `0`，已确认 product positive / product negative 没有新增 routing FN；但 canonical mapping / label extraction 仍未完整接入，因此总体结论必须保持 `REVIEW_NEEDED`，不能宣称完整标签抽取器已经解决。

硬边界保持：

- 不写 production DB。
- 不触发 production upload / reanalysis / credit / 真实 LLM。
- 不开启或扩大 v2 frontstage feature flag。
- 不改变 Results Top10、single review detail、raw export、single-tag download。
- 不启动完整 v3，不接 production frontstage。
- 不用 Step 9.1 结果替换现有 Customer Issue / Customer Label。

## 2. 新增内容

代码：

- `backend_api/app/services/review_signal_shadow.py`
- `backend_api/tests/test_review_signal_step9_1_shadow.py`
- `backend_api/tests/fixtures/review_signal_step9_1_airpods_minimal.json`
- `scripts/review_signal_step9_1_shadow_replay.py`

Artifact：

- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-shadow-summary.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-routing-table.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-fixture-results.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-gold-assimilation.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-routing-projection.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-shadow-fp-fn-comparison.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-phase4-guarded-read-path-plan.json`
- `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-phase4-readiness-checklist.json`

## 3. Schema

最小 `review_signals` 字段：

| Field | 说明 |
|---|---|
| `signal_type` | 片段类型 |
| `polarity` | positive / negative / mixed / neutral |
| `evidence_span` | 当前评论内可定位证据片段 |
| `current_product_scope` | current_product / current_product_context / non_product_context / other_product / accessory_only / shipping_service / unclear / audit_only |
| `route_to` | 该片段进入的业务路由 |
| `confidence` | 0-1 |
| `reason` | 分流原因 |

helper 还会补充本地验证字段：`evidence_start`、`evidence_end`、`evidence_verified`、`route_blocked_reasons`。

## 4. Routing Table

| signal_type | route_to |
|---|---|
| `product_positive` | `user_experience.positive`, `customer_label_candidate` |
| `product_negative` | `user_experience.negative`, `customer_issue_candidate` |
| `audience` | `consumer_profile` |
| `usage_location` | `consumer_profile` |
| `usage_time` | `consumer_profile` |
| `behavior` | `consumer_profile` |
| `purchase_motivation` | `purchase_motives` |
| `expectation` | `unmet_needs` |
| `comparison_or_other_product` | `audit.filter_only` |
| `accessory_only` | `audit.filter_only` |
| `shipping_service` | `audit.filter_only` |
| `generic_or_vague` | `audit.filter_only` |
| `audit_only` | `audit.filter_only` |

Customer Issue shadow candidates 只允许来自 `product_negative`。Customer Label shadow candidates 只允许来自 `product_positive`。

## 5. Fixture 覆盖

AirPods 长评核心片段已覆盖：

| Evidence | signal_type | route_to |
|---|---|---|
| `comfortable` | `product_positive` | `user_experience.positive`, `customer_label_candidate` |
| `fit is secure` | `product_positive` | `user_experience.positive`, `customer_label_candidate` |
| `battery life is great` | `product_positive` | `user_experience.positive`, `customer_label_candidate` |
| `charging indicator is unclear` | `product_negative` | `user_experience.negative`, `customer_issue_candidate` |
| `noise cancellation is poor` | `product_negative` | `user_experience.negative`, `customer_issue_candidate` |
| `fall out when she dances` | `product_negative` | `user_experience.negative`, `customer_issue_candidate` |
| `daughter` / `son` / `kids` | `audience` | `consumer_profile` |
| `around the house` | `usage_location` | `consumer_profile` |
| `homework` / `dance practice` | `behavior` | `consumer_profile` |
| `I bought them because I wanted noise cancellation for a loud environment` | `purchase_motivation` | `purchase_motives` |
| `I expected stronger noise cancellation in loud rooms` | `expectation` | `unmet_needs` |
| `AirPod 3 and 2nd generation Pros` | `comparison_or_other_product` | `audit.filter_only` |
| `case cover is cute` | `accessory_only` | `audit.filter_only` |
| `Overall good` | `generic_or_vague` | `audit.filter_only` |

另外覆盖：

- 5 星评论中同时存在 `product_positive` 和 `product_negative`。
- mixed review 中 `product_positive` 仍进入 Customer Label candidate。
- `usage_time` / `shipping_service` 等边界。
- old/other product、accessory、shipping、generic/vague 默认 audit/filter。

## 6. Phase 1 Minimal Verification (archived)

以下是 Phase 1 最小 AirPods fixture 的历史结果；当前 Phase 1-3 replay 结果见第 12 节。

```bash
python3 -m pytest backend_api/tests/test_review_signal_step9_1_shadow.py -q
```

Original Phase 1 result: `8 passed in 0.07s`

```bash
python3 scripts/review_signal_step9_1_shadow_replay.py
```

Original Phase 1 result:

```json
{"artifact_dir": "tmp/5.9.9-step9.1-review-signal-layer-minimal", "review_count": 4, "signal_count": 28, "status": "PASS"}
```

Additional local checks:

```bash
python3 -m compileall backend_api/app/services/review_signal_shadow.py scripts/review_signal_step9_1_shadow_replay.py
python3 -m ruff check backend_api/app/services/review_signal_shadow.py backend_api/tests/test_review_signal_step9_1_shadow.py scripts/review_signal_step9_1_shadow_replay.py
git diff --check
```

Result: PASS.

## 7. Phase 1 Artifact Summary (archived)

`review-signal-shadow-summary.json` 关键结果：

| Metric | Value |
|---|---:|
| `status` | `PASS_MINIMAL_SHADOW_ONLY` |
| `review_count` | `4` |
| `signal_count` | `28` |
| `non_product_to_issue_label_leakage_count` | `0` |
| `five_star_product_negative_issue_candidate_count` | `3` |
| `customer_issue_candidate_signal_types` | `["product_negative"]` |
| `customer_label_candidate_signal_types` | `["product_positive"]` |

## 8. Phase 2 Gold Assimilation

新增本地片段级 gold builder，覆盖 `36` 条本地 review、`145` 个 fragment。来源明确区分：

| Source kind | Fragment count |
|---|---:|
| `screenshot_derived_gold` | 17 |
| `human_gold_fixture` | 59 |
| `blind_regression_fixture` | 14 |
| `production_readonly_local_copy` | 43 |
| `candidate_pool_reviewed` | 1 |
| `local_shadow_probe` | 11 |

Dataset 覆盖：

| Dataset | Fragment count |
|---|---:|
| `airpods_screenshot_gold` | 17 |
| `airpods_shadow_probe` | 11 |
| `session120_human_gold` | 30 |
| `session121_blind_regression` | 14 |
| `waders_351_400_human_gold` | 29 |
| `session124_readonly_local_copy` | 43 |
| `candidate_pool_reviewed` | 1 |

Gold 口径：

- `session124_readonly_local_copy` 只是生产只读观察的本地副本，不命名为 production gold。
- `candidate:*` 与缺 canonical key 的片段保留为 `extraction_unresolved`，不参与 PASS 型 TP/FP/FN 断言。
- `evidence_not_found_count=0`，所有 gold fragment 的 evidence span 都能在本地 review content 中定位。

## 9. Phase 3 Routing Projection

新增 signal-derived shadow projection，输出：

- `signal_derived_customer_issue_candidates`
- `signal_derived_customer_label_candidates`
- `consumer_profile_signals`
- `purchase_motive_signals`
- `unmet_need_signals`
- `audit_filter_signals`
- `blocked_or_unmatched_occurrences`
- `projection_reasons`

结果：

| Metric | Value |
|---|---:|
| `review_count` | 36 |
| `fragment_count` | 145 |
| `non_product_to_issue_label_leakage_count` | 0 |
| `evidence_not_found_count` | 0 |
| `unresolved_mapping_count` | 14 |

Routing gate 通过的点：

- 只有 `product_negative` 进入 Customer Issue candidate。
- 只有 `product_positive` 进入 Customer Label candidate。
- 5 星 mixed review 中真实 `product_negative` 没有因 rating=5 被过滤。
- mixed review 中真实 `product_positive` 没有因同条 review 存在负向片段被过滤。
- old/other product、accessory-only、shipping/service、generic/vague、audience/context/motivation/expectation 均未进入 issue/label candidate。

## 10. Baseline vs Signal Shadow FP/FN

本轮比较的是本地 curated gold/projection，不是生产回写，也不是完整抽取器替换。

Overall:

| Metric | Baseline | Signal shadow | Delta |
|---|---:|---:|---:|
| Customer Issue TP | 20 | 22 | +2 |
| Customer Issue FP | 9 | 0 | -9 |
| Customer Issue FN | 2 | 0 | -2 |
| Customer Label TP | 36 | 38 | +2 |
| Customer Label FP | 6 | 0 | -6 |
| Customer Label FN | 2 | 0 | -2 |

Split metrics:

| Metric | Issue | Label |
|---|---:|---:|
| `routing_fp` | 9 | 2 |
| `routing_fn` | 0 | 0 |
| `label_extraction_fp` | 0 | 4 |
| `label_extraction_fn` | 2 | 2 |

Routing layer 明确降低的 FP/FN：

- `Breaks Easily`：正向 evidence `hold up well / tear easily` 不再进入 issue。
- `Missing Wader Hanger`：hanger/accessory 片段不再进入 Customer Issue。
- `Poor Traction`：`provide good traction` 不再进入负向 issue。
- `Pocket Not Waterproof`：仅提到 breast pocket 不再进入 waterproof issue。
- `Water Leaks Through`：`no water seeping in` 这类 negated evidence 不再进入 leak issue。
- `not_used_yet`、shipping/service、generic praise、audience/context 不再进入 Customer Label。

仍不能归因给 routing layer 的问题：

- AirPods screenshot product-positive/product-negative 片段没有本地 canonical key，属于 `extraction_unresolved`。
- `candidate:*` reviewed pool / 351-400 needs-new-label 只能进入 reviewable backlog，不能算 display PASS。
- Baseline 中剩余 label FP/FN 仍属于 label extraction / canonical mapping / frontstage display gate 问题，不是 routing gate 已经解决的部分。

## 11. Artifact Summary

`review-signal-shadow-summary.json` 现在汇总 Phase 1-3：

| Metric | Value |
|---|---:|
| `status` | `REVIEW_NEEDED` |
| `local_gold_review_count` | 36 |
| `local_gold_fragment_count` | 145 |
| `non_product_to_issue_label_leakage_count` | 0 |
| `unresolved_mapping_count` | 14 |
| `routing_layer_ready_for_controlled_gray_review` | true |
| `full_label_extraction_ready` | false |

## 12. Verification

```bash
python3 -m pytest \
  backend_api/tests/test_review_signal_step9_1_shadow.py \
  backend_api/tests/test_review_signal_step9_1_gold_assimilation.py \
  -q
```

Result: `16 passed in 0.24s`

```bash
python3 scripts/review_signal_step9_1_shadow_replay.py
```

Result:

```json
{"artifact_dir": "tmp/5.9.9-step9.1-review-signal-layer-minimal", "local_gold_fragment_count": 145, "local_gold_review_count": 36, "non_product_to_issue_label_leakage_count": 0, "review_count": 4, "signal_count": 28, "status": "REVIEW_NEEDED", "unresolved_mapping_count": 14}
```

Additional local checks:

```bash
python3 -m ruff check backend_api/app/services/review_signal_shadow.py backend_api/tests/test_review_signal_step9_1_shadow.py backend_api/tests/test_review_signal_step9_1_gold_assimilation.py scripts/review_signal_step9_1_shadow_replay.py
python3 -m compileall backend_api/app/services/review_signal_shadow.py scripts/review_signal_step9_1_shadow_replay.py
```

Result: PASS.

## 13. Remaining Risk

这是最小验证版，重点是 routing contract，不是完整抽取器：

- 本轮使用本地 fixture 的片段候选，不调用真实 LLM。
- 尚未接入 `review_analyzer/insight_engine.py` 聚合入口。
- 尚未替换 Customer Issue / Customer Label 生成结果。
- 尚未接 Results Top10、single review detail、raw export、single-tag download。
- 尚未解决所有 label extraction / canonical mapping；`unresolved_mapping_count=14`。

下一步才是受控生产部署/灰度评审准备：先把 signal extraction / canonical mapping / display gate 的接入方案单独评审，再决定是否进入 guarded frontstage read path。当前本轮没有执行 production upload、reanalysis、credit、真实 LLM、feature flag、deploy。

## 14. Post-Push Safety Check

Artifact: `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-post-push-safety-check.json`

Result: `PASS_LOCAL_SAFETY_WITH_DEPLOY_TRIGGERED_BY_EXISTING_DEVELOP_WORKFLOW`

Key observations:

- `0bd3f5f` triggered GitHub Actions `CI` run `30613177702` and `Deploy to Production` run `30613177788`; both completed `success`.
- Current `origin/develop` head `99d37dd` also triggered `CI` run `30613430427` and `Deploy to Production` run `30613430602`; both completed `success`.
- This deploy trigger came from the existing `.github/workflows/deploy.yml` `push: develop` rule; this task did not push or deploy.
- Runtime import scan found `review_signal_shadow.py` imported only by the local replay script and Step 9.1 tests. No route, `review_analyzer/insight_engine.py`, worker, or frontstage runtime import was found.
- No workflow file changed in `0bd3f5f` or `99d37dd`; deploy scope was not expanded.
- No production upload, reanalysis, credit path, real LLM call, production DB write, feature flag enablement, frontstage connection, or user-visible result change was performed in this task.

## 15. Unresolved Mapping Review

Artifact: `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-unresolved-mapping-review.json`

Result: `ERIKA_DECISIONS_RECORDED_NOT_LABEL_EXTRACTION_PASS`

The Phase 3 `unresolved_mapping_count=14` was reviewed as the set of current-product Issue/Label candidates whose routing is valid but canonical mapping is not fully settled. Erika finalized the local mapping review as follows:

| Action | Count | Notes |
|---|---:|---|
| `map_to_existing_canonical` | 4 | `comfortable -> comfortable_to_wear`, `battery life is great -> long_battery_life`, `pairing is easy -> easy_to_use`, `These waders are lightweight -> lightweight_waders` |
| `propose_new_candidate_label` | 9 | `poor_noise_cancellation`, `unclear_charging_indicator`, `poor_earbud_fit_stability` x2, `secure_earbud_fit`, `good_sound_quality`, `not_for_hardcore_conditions`, `thin_boot_sole`, `boot_seam_leak` |
| `keep_audit_only` | 1 | `Keep me dryish for the most part` remains audit-only because the evidence is hedged and mixed with later leak context |
| `needs_erika_review` | 0 | Erika selected `candidate:secure_earbud_fit` instead of `fits_as_expected` |

Important boundary: `candidate:*` items are not counted as PASS and no canonical key was fabricated. This artifact classifies the review queue; it does not implement a label extractor or prove frontstage display readiness.

## 16. Phase 4 Guarded Read Path Plan

Artifact: `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-phase4-guarded-read-path-plan.json`

Result: `READY_FOR_ERIKA_REVIEW_DESIGN_ONLY`

Design summary:

- Customer Issue consumes only `product_negative`.
- Customer Label consumes only `product_positive`.
- `audience` / `usage_location` / `usage_time` / `behavior` route to `consumer_profile`.
- `purchase_motivation` routes to `purchase_motives`.
- `expectation` routes to `unmet_needs`.
- `comparison_or_other_product` / `accessory_only` / `shipping_service` / `generic_or_vague` / `audit_only` route to `audit.filter_only`.
- Results Top10, single review detail, raw review export, and single-tag download continue to use only selected product-derived display occurrences; non-product signals may appear only in audit/debug surfaces.
- Default read path remains `v1_current`; the proposed guarded path consumes stored shadow only, requires explicit feature flag scope, fails closed on invalid config or missing stored shadow, and supports rollback plus global/scoped kill switch.
- Observability must report read-path selection, blocked reasons, candidate/unknown mapping, no-stored-shadow, display-gate exclusions, and routing leakage. Required thresholds: routing leakage `0`, candidate-key frontstage count `0`, evidence-not-found frontstage count `0`.

This is a design artifact only. No production runtime, frontstage consumer, upload path, reanalysis path, credit path, LLM path, feature flag, stored shadow write, or user-visible result was changed.

## 17. Phase 4 Readiness Checklist

Artifact: `tmp/5.9.9-step9.1-review-signal-layer-minimal/review-signal-phase4-readiness-checklist.json`

Result: `READY_FOR_IMPLEMENTATION_AUTHORIZATION`

This is implementation prep only. It does not implement the guarded read path and does not authorize Phase 5.

Readiness gates now explicitly align with `PROGRESS_V2.md`:

- Customer Issue can consume only `product_negative`.
- Customer Label can consume only `product_positive`.
- `candidate:*` remains non-canonical, does not count as PASS, and never enters frontstage.
- `audit_only` and `audit.filter_only` never enter Results Top10, single review issue/label chips, main raw export issue/label columns, or single-tag download.
- `consumer_profile`, `purchase_motives`, `unmet_needs`, and `audit.filter_only` routes stay isolated from Customer Issue / Customer Label display.
- Results Top10, single review detail, raw export, and single-tag download must share the same selected product-derived display occurrence set.
- Feature flag default remains off and requires explicit scoped Erika authorization before runtime/frontstage connection.
- The guarded read path can consume stored shadow/read model only; runtime shadow generation is not allowed in frontstage reads.
- Global and scoped rollback / kill switch must force `v1_current`.
- Observability must include read-path selection, blocked reason counters, candidate-key frontstage count, routing leakage, stored shadow availability, rollback, and kill switch counters.
- Invalid config, missing stored shadow, unresolved mapping, `candidate:*`, display gate failure, or non-product signal must fail closed to `v1_current` or exclude the occurrence.

Phase 4 is ready for Erika to authorize implementation, but `phase4_implementation_authorization_granted=false` in this run. Phase 5 production truth check replay remains blocked until Phase 4 is implemented, verified, and Erika separately authorizes production replay.

Unchanged boundaries for this readiness pass:

- No production DB write.
- No upload, reanalysis, credit, or real LLM.
- No feature flag enablement.
- No production frontstage connection.
- No user-visible result change.
- No Phase 5 execution.
- No push/deploy.
