# 5.9.9 Step 9: Erika-led production truth check

日期：2026-07-31

状态：`FIX_REQUIRED_BEFORE_GRAY_EXPAND`

## 1. 结论

Step 9 第一轮已完成生产真值验收的旁路 baseline、session124 production read-only observation、Erika 导出文件旁路检查，以及发布隔离复核；当前不能判 `PASS`。

原因有两类：

- 发布隔离破口：v3-lite commit `a238c22` 已进入 `develop`，而 `.github/workflows/deploy.yml` 会在 push 到 `develop` 后自动部署生产；公开 GitHub Actions 只读查询确认 `a238c22` 和后续包含它的 `006734c` 均部署成功。
- session124 真实生产结果需要修复：Top10/raw export 出现多条高置信疑似错标或 evidence 极性问题，例如 `Missing Wader Hanger` 使用 hanger works great 作为负向 evidence、`Poor Traction` 使用 good traction 作为负向 evidence、`Pocket Not Waterproof` 只有 breast pocket evidence。

因此本轮结论为：

```text
FIX_REQUIRED_BEFORE_GRAY_EXPAND
```

这不是要求立即 rollback 或 kill switch；它表示不得扩大 v2 gray exposure，必须先恢复发布隔离并处理 session124 的真实标签质量问题。

## 2. 硬边界

本轮未执行以下动作：

- 未触发 production upload。
- 未触发 production reanalysis。
- 未写 production DB。
- 未消耗 credit。
- 未调用真实 LLM。
- 未开启 production feature flag 或 scope。
- 未执行 rollback。
- 未执行 kill switch。

Erika 可以通过正常产品界面自行上传真实样本并判断页面结果。Codex 的职责是旁路记录、错标/漏标归因、observability 解释、rollback/kill switch 建议和文档沉淀；Codex 不替代 Erika 的产品验收。

## 3. 第一轮 Baseline

已确认 Step 7.8 go/no-go 仍是：

```text
GO_READY_FOR_ERIKA_AUTHORIZED_GRAY_RUN_ONLY
```

生产开启相关状态：

| 项 | 状态 |
|---|---|
| production feature flag 当前开启 | `false` |
| production gray run 已执行 | `false` |
| 需要 Erika 单独授权 | `true` |
| AI 冷灰度可单独决定 PASS | `false` |

Step 7.6 / 7.7 / 7.8 / 8 的安全 baseline：

| Baseline | Status | Evidence |
|---|---|---|
| feature flag default-off | `PASS` | Step 7.8 acceptance pack |
| readiness dry-run | `PASS` | Step 7.8 acceptance pack |
| stored shadow/read model contract | `PASS` | Step 7.6 stored shadow available `8`, missing `0` |
| observability counters | `PASS` | Step 7.6 counters present |
| rollback drill | `PASS` | Step 7.7 rollback drill case count `6`, violations `[]` |
| kill switch drill | `PASS` | Step 7.7 / 7.6 counters |
| vector memory display guard | `PASS` | Step 8 vector result cannot decide frontstage display |

## 4. Stored Shadow / Read Model

Step 7.6 本地 contract 已证明 read path 能区分：

- `v1_current`
- `v2_shadow`
- flag off
- fixture gate fail
- config invalid
- rollback
- kill switch
- stored shadow availability

Step 7.6 observability counters：

| Counter | Value |
|---|---:|
| `v1_selected_count` | `7` |
| `v2_selected_count` | `1` |
| `blocked_by_flag_off` | `1` |
| `blocked_by_fixture_gate` | `1` |
| `blocked_by_config_invalid` | `1` |
| `rollback_selected_count` | `2` |
| `kill_switch_selected_count` | `2` |
| `stored_shadow_available_count` | `8` |
| `stored_shadow_missing_count` | `0` |

但这仍只是已完成的本地/contract baseline。当前 Step 9 还没有一个 Erika 授权的真实生产样本/session scope 作为本轮 truth check 对象。

## 5. 必查四条路径

Step 9 后续必须检查以下路径，且检查人必须是 Erika：

| Path | 当前状态 | 验收重点 |
|---|---|---|
| Results Top10 | `PENDING_ERIKA_REAL_PAGE_REVIEW` | Top issue/highlight 是否符合产品直觉；evidence 是否指向真实含义；candidate/audit/unknown 不泄漏 |
| single review detail | `PENDING_ERIKA_REAL_PAGE_REVIEW` | 单条评论标签是否错标/漏标；evidence 是否可定位可信 |
| raw review export | `PENDING_ERIKA_REAL_PAGE_REVIEW` | raw 导出是否保持 verified display 口径；不暴露 propagated/unverified/audit-only 结果 |
| single-tag download | `PENDING_ERIKA_REAL_PAGE_REVIEW` | 单标签下载内的每行是否真实支持该 tag；记录 FP/FN |

## 6. PASS 口径

Step 9 `PASS` 必须同时满足：

- Erika 已查看真实生产样本页面。
- Results Top10、single review detail、raw review export、single-tag download 四条路径均已检查。
- Erika 明确确认错标可接受或为 `0`。
- Erika 明确确认漏标可接受或为 `0`。
- Erika 明确确认 evidence 可信。
- Erika 明确确认标签命名和归类符合产品直觉。
- 若启用 v2 read path，则仅限 Erika 单独授权的 scope，且 stored verified v2 display 可用。

AI 冷灰度、本地 replay、focused tests 只能作为安全网，不能单独判 PASS。

## 7. Rollback / Kill Switch 建议

当前没有触发 rollback 或 kill switch 的证据，因为还没有 Step 9 真实生产样本验收结果。

若 Erika 在真实页面验收中发现以下情况，建议停止扩大灰度，并进入对应结论：

| 发现 | 建议结论 |
|---|---|
| v2 scope 已开启且出现 P0 错标、candidate/audit 泄漏、unknown label 展示、stored shadow 缺失仍展示 v2 | `ROLLBACK_REQUIRED` |
| 未开启 v2 scope，但真实样本暴露错标/漏标/evidence 问题 | `FIX_REQUIRED_BEFORE_GRAY_EXPAND` |
| 无法取得真实样本、stored shadow 或 Erika 人工判断 | `BLOCKED_NEEDS_AUTH_OR_STORED_SHADOW` |
| 四条路径均通过 Erika 人工判断，且无 P0/阻断项 | `PASS` |

## 8. Artifact

输出：

- `tmp/5.9.9-step9-erika-led-production-truth-check/production-truth-check-observation.json`

Artifact 记录：

- 当前结论：`FIX_REQUIRED_BEFORE_GRAY_EXPAND`
- feature flag baseline
- stored shadow/read model baseline
- observability counters
- rollback/kill switch baseline
- 四条必查路径 checklist
- Erika truth acceptance required fields
- Step 9 safety flags

## 9. 下一步

下一步需要 Erika 提供真实产品验收输入之一：

1. Erika 通过正常产品界面上传或选择真实生产样本，并人工检查四条路径。
2. Erika 单独授权 Codex 对具体 production session/scope 做只读观察。
3. 若要开启任何 v2 frontstage scope，Erika 必须单独授权具体 feature flag/scope 动作。

在此之前，Step 9 保持：

```text
FIX_REQUIRED_BEFORE_GRAY_EXPAND
```

## 10. Erika 参与步骤

这部分是 Erika 的实际操作清单。只需要使用正常产品界面，不需要碰数据库、feature flag、后台配置或任何开发工具。

### 10.1 准备样本

建议第一轮用 `20-50` 条真实评论；更推荐 `50` 条。样本最好满足：

- 不是 session120 / session121 / session122 或本地 gold fixture 用过的旧样本。
- 优先从 `outdoor/waders` 开始，因为这是当前 L3 验证最充分的 scope。
- 混入明确差评、明确好评、中性/模糊短评、旧产品/他牌/配件/未实际使用语境。
- 尽量包含 size/fit、waterproof/leak、durability、value、comfort 这类容易影响产品判断的内容。

记录：

```text
sample_file=
row_count=
product=
category/sub_category=
previously_uploaded=no/yes/unknown
```

### 10.2 通过正常产品界面上传或选择 session

Erika 自己在产品界面上传或选择样本。Codex 不代替上传，也不触发 reanalysis。

记录：

```text
session_id=
page_url=
upload_time=
job_status=
visible_error=
```

如果只是选择已有真实 session，也记录 session_id 和页面 URL。

### 10.3 检查 Results Top10

打开结果总览页，重点看 Top Customer Issue / Customer Label。

判断问题：

- Top 标签是否像真实卖家会关心的问题/亮点。
- 是否出现明显错标。
- 是否漏掉肉眼很明显的高频问题。
- evidence 是否真的支持该标签。
- 是否出现 candidate / audit / unknown 这类不该用户可见的东西。

记录：

```text
Top10 checked=yes/no
correct labels=
wrong labels=
missing obvious labels=
suspicious evidence=
candidate/audit/unknown leakage=yes/no
```

### 10.4 检查 single review detail

至少打开 `10` 条单条评论详情。优先看：

- Top10 重要标签背后的评论。
- 看起来可疑的标签。
- 短评、泛泛好评、旧产品/他牌/配件/未实际使用语境。
- 明确提到漏水、防水、尺码、耐用、性价比的评论。

记录格式：

```text
review row/id=
shown labels=
wrong labels=
missing labels=
evidence credible=yes/no
product intuition note=
```

### 10.5 检查 raw review export

下载 raw review export，抽查页面和导出是否一致。

重点看：

- raw 导出是否保持 verified display 口径。
- 是否把 unverified evidence、cluster propagated、candidate-only、audit-only 内容暴露成用户可见结果。
- evidence 是否仍能对应到原评论。

记录：

```text
raw export checked=yes/no
export file=
page/export mismatch=
unverified/candidate/audit leakage=
evidence issues=
```

### 10.6 检查 single-tag download

选 `2-4` 个重要标签下载单标签文件。建议至少覆盖：

- 一个 issue 标签。
- 一个 highlight 标签。
- 一个你最怀疑会错标的标签。

对每个 tag 记录：

```text
tag=
download row count=
false positive rows=
obvious missing rows=
tag product-usable=yes/no
notes=
```

### 10.7 给出 Erika 人工结论

最终只选一个：

| 结论 | 什么时候选 |
|---|---|
| `PASS` | 四条路径都检查完；没有 P0；evidence 可信；标签符合产品直觉；错标/漏标在样本内可接受 |
| `FIX_REQUIRED_BEFORE_GRAY_EXPAND` | 没有需要立刻 rollback 的线上伤害，但发现错标、漏标或 evidence 问题，扩大灰度前必须修 |
| `ROLLBACK_REQUIRED` | 如果 v2 scope 已开启，且出现 P0 错标、candidate/audit/unknown 泄漏、stored shadow 缺失仍展示 v2 等用户可见伤害 |
| `BLOCKED_NEEDS_AUTH_OR_STORED_SHADOW` | 样本、session、页面、stored shadow 或 Erika 人工判断不完整 |

可直接把下面这段填完发给 Codex：

```text
sample_file=
row_count=
product=
category/sub_category=
previously_uploaded=

session_id=
page_url=
upload_time=
job_status=

Top10 checked=
Top10 wrong labels=
Top10 missing labels=
Top10 suspicious evidence=

single review detail checked rows=
detail wrong labels=
detail missing labels=
detail evidence issues=

raw export checked=
raw export file=
raw export mismatches=
raw export leakage=

single-tag downloads checked=
single-tag false positives=
single-tag missing obvious rows=

mislabels acceptable=yes/no
missing labels acceptable=yes/no
evidence trustworthy=yes/no
labels match product intuition=yes/no
final verdict=PASS/FIX_REQUIRED_BEFORE_GRAY_EXPAND/ROLLBACK_REQUIRED/BLOCKED_NEEDS_AUTH_OR_STORED_SHADOW
```

对应 artifact：

- `tmp/5.9.9-step9-erika-led-production-truth-check/erika-production-truth-check-checklist.json`

## 11. Session 124 Intake

Erika 已提供生产结果页：

```text
https://www.clueai-reviewlens.com/analysis/results?product_id=TIDEWE-%E4%B8%8B%E6%B0%B4%E6%9C%8D-WD001&range=default&session_id=124
```

解析结果：

| Field | Value |
|---|---|
| `session_id` | `124` |
| `product_id` | `TIDEWE-下水服-WD001` |
| `range` | `default` |

Codex 已做一次只读 HTTP HEAD 可达性检查：

| Item | Result |
|---|---|
| HTTP status | `307` |
| Redirect | `/login` |
| Interpretation | 生产路由可达，但页面内容需要 Erika 登录态；Codex 未看到页面内 Customer Label / Customer Issue 结果 |

当前 session 124 状态：

```text
PENDING_ERIKA_PAGE_REVIEW_OR_READONLY_AUTH
```

下一步有两个选择：

1. Erika 继续在已登录浏览器里按第 10 节完成四条路径人工验收，并把记录发给 Codex。
2. Erika 明确授权 Codex 对 `session_id=124` 做 production read-only observation。

如选择第 2 项，可使用这句授权语：

```text
授权 Codex 对 session_id=124 做 production read-only observation；禁止写库、重分析、credit、LLM、feature flag/scope、rollback、kill switch。
```

对应 artifact：

- `tmp/5.9.9-step9-erika-led-production-truth-check/session124-intake.json`

## 12. Session 124 Top10 XLSX Side-channel Review

Erika 提供本地文件：

```text
/Users/zhangxi/Desktop/TIDEWE-下水服-WD001_401-450-customer label-customer issue.xlsx
```

Codex 已做本地只读解析。文件未被修改。

| Item | Value |
|---|---|
| SHA256 | `7396be5b7b99cd267b727ca9db9d1385ec95bee70bddd57a13640172cd625f46` |
| Sheets | `Positive Feedback TOP10`, `Negative Feedback TOP10`, `AI Notice` |
| Positive Top10 rows | `10` |
| Negative Top10 rows | `10` |
| 覆盖路径 | Results Top10 部分覆盖 |
| 未覆盖路径 | single review detail、raw review export、single-tag download |

Codex preliminary conclusion:

```text
FIX_REQUIRED_BEFORE_GRAY_EXPAND_CANDIDATE
```

正式 Step 9 结论已升级为：

```text
FIX_REQUIRED_BEFORE_GRAY_EXPAND
```

原因：这份文件只覆盖 Top10 汇总，不能单独完成 PASS；后续 production read-only observation 和 release-isolation check 又确认了质量与发布隔离问题。

### 12.1 Positive Top10 初步观察

Positive Top10 从 representative evidence 看整体较合理：

- `Good Value for the Price`
- `Fits as Expected`
- `Keeps Water Out`
- `Good Material Quality`
- `Holds Up Well`
- `Comfortable To Wear`
- `Good Traction`
- `Lightweight Waders`

需要 Erika 产品判断的轻微问题：

- `Useful Storage Space` 和 `Easy To Use` 都围绕 pocket/hanger/storage，可能存在标签重叠，不一定是 P0，但适合进入 catalog cleanup。

### 12.2 Negative Top10 疑似问题

以下是 Codex side-channel 初筛，不替代 Erika 结论。

| Rank | Issue | Preliminary judgment | Why suspicious |
|---:|---|---|---|
| 2 | `Breaks Easily` | `MIXED_EVIDENCE_WITH_NEGATION_FALSE_POSITIVE_RISK` | evidence 混入 `no water seeping in`、`don’t tear easily` 等非支持或反向短语 |
| 6 | `Missing Wader Hanger` | `LIKELY_FALSE_POSITIVE` | evidence 是 `The hanger it comes with works great for drying and storage`，更像 hanger 存在且好用 |
| 7 | `Pocket Not Waterproof` | `LIKELY_FALSE_POSITIVE` | evidence 只有 `There's a little breast pocket`，没有 pocket 漏水或不防水 |
| 8 | `Poor Traction` | `LIKELY_FALSE_POSITIVE_OR_POLARITY_FLIP` | evidence 包含 `provide good traction on slippery rocks`，更像正向 `Good Traction` |
| 9 | `Size/Fit Problem` | `LIKELY_NOT_PRODUCT_ISSUE` | evidence 是 `I ordered the wrong size`，可能是用户下单错误，不是产品尺码问题 |

### 12.3 Erika 下一步重点

请优先在页面和下载文件里复核这几项：

1. 打开 `Breaks Easily`、`Missing Wader Hanger`、`Pocket Not Waterproof`、`Poor Traction`、`Size/Fit Problem` 背后的 single review detail。
2. 下载 `Pocket Not Waterproof`、`Missing Wader Hanger`、`Poor Traction`、`Breaks Easily` 的 single-tag 文件。
3. 检查 raw review export 里这些 issue 是否作为 verified user-facing 结果出现。
4. 给出 Erika 人工判断：这些是可接受噪音、必须修复，还是已构成线上用户可见伤害。

对应 artifact：

- `tmp/5.9.9-step9-erika-led-production-truth-check/session124-top10-xlsx-sidechannel-review.json`

## 13. Session124 Production Read-only Observation

Erika 已明确授权：

```text
授权 Codex 对 session_id=124 做 production read-only observation；禁止写库、重分析、credit、LLM、feature flag/scope、rollback、kill switch。
```

Codex 已执行 production read-only observation，连接状态确认：

| Flag | Value |
|---|---|
| `transaction_read_only` | `on` |
| `default_transaction_read_only` | `on` |
| `pg_is_in_recovery` | `false` |

生产健康观察：

| Item | Value |
|---|---:|
| `session_id` | `124` |
| `job_id` | `105` |
| `job_status` | `done` |
| `comments` | `50` |
| `processed` | `50` |
| `analyzed` | `50` |
| `distinct_content_hashes` | `50` |
| `comment_cache_rows` | `1` |
| `review_analyze_delta` | `-50` |
| Codex 触发 credit / LLM | `false` |

DB checks：

| Check | Status |
|---|---|
| comments expected/processed/analyzed | `PASS` |
| no L1 cache rows | `FAIL` |
| single review analyze ledger delta | `PASS` |
| review pool same product backfill only session hashes | `PASS` |

质量观察：

| Finding | Count |
|---|---:|
| evidence not found | `0` |
| no evidence display | `0` |
| cluster propagated display | `0` |
| water false positive | `1` |
| water false negative candidate | `1` |
| four-pack context issue | `2` |
| negative-label/highlight context conflict | `1` |

高置信用户可见问题：

| Path | Row | Label | Issue |
|---|---|---|---|
| Top10/raw export | raw No.3 | `Pocket Not Waterproof` | evidence 只有 `There's a little breast pocket`，不支持“不防水” |
| Top10/raw export | raw No.42 | `Poor Traction` | evidence 是 `provide good traction on slippery rocks`，极性反了 |
| Top10/raw export | raw No.43 | `Missing Wader Hanger` | evidence 是 hanger works great，和“缺失”相反 |
| Top10/raw export | raw No.49 | `Breaks Easily` | evidence 包含 `don’t tear easily`，否定词误伤 |
| raw export | raw No.28 | `Water Leaks Through` | 同评里有 `no water seeping in`，疑似 sweat/front pouch context 污染 |
| raw export | raw No.22 | `Fits as Expected` / `Comfortable To Wear` | update 后强负向，初始正向标签可能误导 |
| Top10/raw export | raw No.14 | `Size/Fit Problem` | `I ordered the wrong size` 可能是用户下错尺码，不一定是产品问题 |

对应 artifact：

- `tmp/5.9.9-step9-erika-led-production-truth-check/session124-production-readonly-observation.json`
- `tmp/5.9.9-step9-erika-led-production-truth-check/session124-readonly/acceptance-summary.json`

## 14. Release Isolation Incident

Step 9 原边界要求 v3-lite 与 Step 9 发布隔离；但当前实际情况不满足。

只读证据：

| Evidence | Value |
|---|---|
| v3-lite commit | `a238c22 Add v3-lite customer insights shadow` |
| current `origin/develop` | `006734c Record step9 session124 intake` |
| `006734c` contains `a238c22` | `true` |
| deploy trigger | push to `develop` |
| deploy behavior | `git reset --hard origin/develop` + rebuild containers |
| GitHub Actions run for `a238c22` | `30605016872`, `success` |
| GitHub Actions run for `006734c` | `30605071916`, `success` |

Runtime import check:

| Check | Result |
|---|---|
| backend/worker/frontstage runtime import found | `false` |
| imports found | tests and shadow replay script only |

Interpretation:

- v3-lite runtime path appears inactive.
- But v3-lite code likely entered the production deployment package.
- Therefore Step 9 can no longer claim strict release isolation.

Recommendation:

1. Freeze v2 gray expansion.
2. Do not claim Step 9 `PASS`.
3. Create a non-destructive cleanup/revert commit or move v3-lite to an isolated branch.
4. Deploy a cleaned develop package.
5. Resume Step 9 after release isolation is restored.

对应 artifact：

- `tmp/5.9.9-step9-erika-led-production-truth-check/release-isolation-incident.json`

## 15. Release Isolation Cleanup

按 Erika 授权，已采用非破坏性迁出方案：

| Item | Value |
|---|---|
| isolated branch | `feature/5.9.9-v3-lite-shadow-only` |
| branch pushed to origin | `true` |
| branch base | `006734c` |
| develop cleanup status | pending commit/deploy verification |

从 `develop` 工作区移除的 tracked v3-lite 文件：

- `backend_api/app/services/customer_insights_v3_lite.py`
- `backend_api/tests/test_customer_insights_v3_lite.py`
- `scripts/customer_insights_v3_lite_shadow_replay.py`
- `docs/5.9.9-v3-lite-customer-insights-shadow.md`

保留说明：

- v3-lite 没有被销毁，已保存在远端隔离分支。
- `tmp/5.9.9-v3-lite-customer-insights-shadow/` 是本地未跟踪 artifact，不会随 cleanup commit 进入生产包。
- cleanup commit push 到 `develop` 后，现有 deploy workflow 会自动部署 clean develop；必须确认 GitHub Actions 成功后，Step 9 才能继续。
