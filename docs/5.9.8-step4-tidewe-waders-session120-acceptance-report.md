# 5.9.8 Step 4: TIDEWE waders session 120 50 条盲测只读验收

日期：2026-07-29

## 结论

`REVIEW_NEEDED`。

这次 50 条样本确实比 session119 更接近盲测，但不能作为最终 PASS：

- 生产 DB 显示 50 条里有 47 条无 comment cache，另有 2 条 L1 global、1 条 L2 cache。缓存样本是短文本 `Good`、`Perfect!`、`Good quality`。
- Erika 下载的 raw review 文件仍是旧 29 列，没有 `Audit ...` 分列。
- 真实下载文件仍把 propagated / evidence false 候选混在前台字段里：issue `evidence=false` 90 个、issue `cluster=true` 90 个；highlight `evidence=false` 24 个、highlight `cluster=true` 24 个。
- 新样本暴露了 3 个本地追加修复点：row 27 `boots got filled with water` 漏标、row 31 `can't say whether or not they leak` 误标、row 28 `Cheap and durable` 同时误标 `feels_thin_and_flimsy`。

本地最新规则修复后，使用 session120 生产 DB 只读回放得到：`validation.status=PASS`。这表示生成/展示语义在本地最新代码下已经覆盖 session120 hard cases，但当前线上结果页和下载文件仍不能判 PASS。

## 范围与边界

- 结果页：`https://www.clueai-reviewlens.com/analysis/results?product_id=TIDEWE-%E4%B8%8B%E6%B0%B4%E6%9C%8D-WD001&range=default&session_id=120`
- Erika 下载文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-session_id=120.xlsx`
- Codex 操作：生产 DB 只读查询、本地等价导出、读取 Erika 下载 XLSX、本地代码修复、fixture/test 更新、文档更新。
- Codex 未执行：生产上传、生产删除、production `review_pool` 清理、production reanalysis、production export API、production LLM/credit 触发。

## 生产只读审计

| 项目 | 结果 |
|---|---|
| session | `120` |
| job | `101` |
| rows | 50 comments / 50 processed / 50 analyzed / 50 normalized dates |
| content hash | 50 distinct hashes，无 session 内重复 |
| comment cache | 47 无缓存；2 L1 global；1 L2 |
| cache samples | `Good` = L1 global；`Perfect!` = L2；`Good quality` = L1 global |
| review_analyze credit | 单条 `delta=-50`，ref `101` |
| llm_usage | 17 rows；14 non-cache，3 cache；cost `0.088783` |
| review_pool backfill | same product since session only same-batch hashes；outside_batch_rows=0 |
| Codex 只读连接 | `transaction_read_only=on` |

判定：本次不是 100% non-cache；但缓存命中只发生在 3 条极短泛评，不解释 session120 的水漏语义问题。

## Erika 下载文件审计

文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-session_id=120.xlsx`

| 项目 | 结果 |
|---|---|
| sheet | `Raw Reviews`, `Export Info` |
| rows | 50 |
| raw headers | 旧 29 列 |
| `Audit ...` 分列 | 缺失 |
| issue evidence false | 90 |
| issue cluster true | 90 |
| highlight evidence false | 24 |
| highlight cluster true | 24 |

下载文件里的污染高频：

| 类型 | key | count |
|---|---|---:|
| issue | `feels_thin_and_flimsy` | 32 |
| issue | `not_breathable` | 30 |
| issue | `runs_too_large` | 30 |
| highlight | `holds_up_well` | 26 |

这些大多来自同一条 propagated evidence：`the pvc is thin but does seem durable`、`you will probably end up sweating a good bit`、`the shoulder straps are always a little loose`。它们不应出现在 raw 前台字段。

## 新发现 hard cases

| row | 原文要点 | 生产当前问题 | 本地最新规则 |
|---:|---|---|---|
| 3 | `not yet tested in water so cannot comment on its leakproof quality` | 下载中混入 propagated 四件套；本地旧规则会误触 `keeps_water_out` | 输出 `fits_as_expected` + `not_used_yet`；不输出 `keeps_water_out` |
| 27 | `boots got filled with water` / `NOT water proof` | `water_leaks_through` 漏标 | 召回 `water_leaks_through`，evidence=`boots got filled with water` |
| 28 | `Cheap and durable` | 同时出现 `feels_thin_and_flimsy` 与 `holds_up_well` | 只保留 `holds_up_well`；`durable` 不能作为材质差 evidence |
| 31 | `never wore them in the water so i can't say whether or not they leak` | 误标 `water_leaks_through` | 不输出 `water_leaks_through`；输出 `not_used_yet` |

## 本地最新规则回放

artifact：

- summary：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session120-readonly/acceptance-summary.json`
- local results：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session120-readonly/session-results.json`
- local raw equivalent：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session120-readonly/frontend-raw-reviews-equivalent.xlsx`
- local backend full export：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session120-readonly/backend-full-export.xlsx`

本地最新规则语义结果：

| 项目 | 结果 |
|---|---|
| validation.status | `PASS` |
| Top violations | 0 |
| single-detail no evidence display | 0 |
| single-detail cluster propagated display | 0 |
| evidence not found | 0 |
| water false positive | 0 |
| water false negative | 0 |
| pocket false negative candidates | 0 |
| four-pack bad rows | 0 |
| negative label bad rows | 0 |

关键 Top：

| 类型 | key | count/evidence |
|---|---|---|
| issue | `water_leaks_through` | 3；`started leaking already`、`leak`、`boots got filled with water` |
| highlight | `keeps_water_out` | 2；`No leaks`、`no leaks` |
| highlight | `fits_as_expected` | 2；`fitted me perfectly`、`Fit great` |
| highlight | `not_used_yet` | 2；`cannot comment on its leakproof`、`never wore them in the water...leak` |

## 本地代码修复

追加修复：

- `not water proof`、`boots got filled with water` 进入真实当前产品漏水召回。
- `not yet tested in water`、`cannot comment on leakproof quality`、`can't say whether or not they leak`、`never wore them in the water` 阻断 `water_leaks_through` 与 `keeps_water_out`。
- `feels_thin_and_flimsy` 必须有 thin / flimsy / not sturdy / cheap material 等合法 evidence；`durable` 不得反向解释为材质差。
- session120 row 3/27/28/31 加入 Step 4 gold fixture。

## 本地验证

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_step4_gold.py -q
```

结果：PASS，3 passed。

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_step4_gold.py backend_api/tests/test_customer_label_waders_step2_gating.py backend_api/tests/test_specific_issue.py backend_api/tests/test_customer_label_phase6_validation.py backend_api/tests/test_export_customer_label_phase5.py backend_api/tests/test_customer_label_50u_readiness.py backend_api/tests/test_outdoor_waders_taxonomy.py -q
```

结果：PASS，73 passed in 3.16s。

```bash
npm run typecheck
```

结果：PASS。

## 下一次验收标准

当前 session120 不能 PASS。下一次需要等本地追加修复部署后，再由 Erika 上传新的 50 条或重新用前端生成新 session。

下一次必须满足：

- raw 下载出现 `Audit ...` 分列。
- raw 前台字段无 `evidence=false` / `cluster=true`。
- `water_leaks_through` 只包含当前产品真实漏水。
- `not yet tested` / `cannot comment` / `can't say whether they leak` 不触发 `water_leaks_through` 或 `keeps_water_out`。
- `boots got filled with water` 必须召回 `water_leaks_through`。
- `durable` 不得触发 `feels_thin_and_flimsy`。
