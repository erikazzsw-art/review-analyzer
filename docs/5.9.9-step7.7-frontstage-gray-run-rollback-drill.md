# 5.9.9 Step 7.7: v2 frontstage gray-run runbook / rollback drill

日期：2026-07-31

状态：`PASS`

## 1. 结论

已新增 Customer Label v2 frontstage 的只读灰度 runbook 与本地 rollback drill artifact。

本轮只做 runbook、只读回滚演练、focused tests、artifact 和文档：

- 不开启生产 feature flag。
- 不执行生产灰度。
- 不触发 production upload。
- 不触发 production reanalysis。
- 不写 production DB。
- 不消耗 credit。
- 不调用真实 LLM。
- 不 push。

生产操作必须由 Erika 单独明确授权；本报告不构成生产开启授权。

核心新增：

- `backend_api/app/services/customer_label_v2_frontstage_runbook.py`
- `customer_label_v2_frontstage_gray_run_runbook()`
- `build_customer_label_v2_frontstage_rollback_drill_report(...)`
- `backend_api/tests/test_customer_label_v2_frontstage_runbook.py`
- `tmp/5.9.9-step7.7-frontstage-gray-run-rollback-drill/gray-run-rollback-drill.json`

## 2. Gray-run Runbook

灰度步骤只定义，不执行：

| Step | Scope | Expected |
|---|---|---|
| `0_percent_baseline` | all traffic | `v1_current` |
| `read_only_stored_shadow_audit` | local/stored shadow payloads only | `v1_current` |
| `scoped_session_gray` | single authorized session | v2 only for authorized session + stored verified display |
| `scoped_sub_category_gray` | `outdoor/waders` | v2 only for L3 waders |
| `scoped_category_observation` | category envelope | L3-only; L1/L2 remain blocked |

Hard stops:

- invalid config
- candidate/audit layer selected
- unknown label selected
- stored shadow missing for enabled scope
- replay P0 above zero
- six focus label FP/FN above zero

## 3. Rollback Drill

本地 dry-run 覆盖：

| Case | Expected path |
|---|---|
| baseline v2 selected | `v2_shadow` |
| global rollback | `v1_current` |
| scoped rollback | `v1_current` |
| global kill switch | `v1_current` |
| scoped kill switch | `v1_current` |
| flag off | `v1_current` |

Artifact result:

- `status=PASS`
- `case_count=6`
- `selected_read_paths={"v1_current": 5, "v2_shadow": 1}`
- `rollback_selected_count=2`
- `kill_switch_selected_count=2`
- `case_expectation_violations=[]`

## 4. Artifact

生成：

- `tmp/5.9.9-step7.7-frontstage-gray-run-rollback-drill/gray-run-rollback-drill.json`

Artifact safety flags:

- `production_feature_flag_enabled=false`
- `production_gray_run_executed=false`
- `production_upload=false`
- `production_write_path=false`
- `production_db_write=false`
- `credit_consumed=false`
- `llm_called=false`

## 5. Verification

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_frontstage_runbook.py -q
```

Result: `3 passed in 0.28s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_frontstage_config.py backend_api/tests/test_customer_label_v2_frontstage_readiness.py backend_api/tests/test_customer_label_v2_frontstage_runbook.py -q
```

Result: `19 passed in 0.27s`

```bash
python3 scripts/customer_label_v2_waders_shadow_replay.py
```

Result: `PASS`, P0=`0`

## 6. Remaining Risk

- Runbook 只定义 0% 到 scoped session/category/sub_category 的灰度步骤，未执行生产开启。
- Rollback drill 是本地 dry-run，不等同于线上指标面板或真实流量演练。
- 生产 feature flag 仍未开启；任何生产 scope、rollback 或 kill switch 操作都必须由 Erika 单独授权。
