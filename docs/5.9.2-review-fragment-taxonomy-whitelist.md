# 5.9.2 Taxonomy 白名单契约

日期：2026-08-03

目标：在 5.9 新主线里增加纯 taxonomy whitelist 校验能力，防止 AI 把当前 `sub_category` 之外的自由标签直接送入正式产品问题 / 亮点聚合。本阶段不接入 active upload、worker、results、export 或 frontend。

## Taxonomy 来源

正式来源是已有 `category_aspect_taxonomy` 表，通过 `backend_api/app/services/taxonomy_loader.py` 的 `resolve_aspects(sub_category)` 读取。

5.9.2 新模块为 `backend_api/app/services/review_fragment_taxonomy_whitelist.py`：

- 新命名入口：`review_fragment_taxonomy_*`。
- 不使用 `customer_label_v2_*` 或 `review_signal_*` 作为主入口。
- 复用现有 taxonomy loader，不新建标签库。
- `taxonomy_loader` 的 fallback base aspect 只用于旧 prompt 兼容；在 5.9.2 whitelist 中，`hit=False` 一律视为 `taxonomy_missing`，不得正式聚合。

测试 fixture 使用 `data/taxonomy/v1.0/outdoor/waders.yaml` 和 `data/taxonomy/v1.0/home/床架.yaml` 模拟 `category_aspect_taxonomy` 行。

## sub_category 解析规则

- 以当前产品 review-level payload 的 `sub_category` 为准。
- `category` 只保留用于审计和人工表格，不作为 aspect 白名单来源。
- `sub_category` 为空、缺失或无法判断时，返回 `taxonomy_missing`。
- `resolve_aspects(sub_category)` 返回 `hit=False` 时，返回 `taxonomy_missing`，即使 loader 给了 fallback base aspect。
- 白名单只保留正式 aspect key：排除空 key、`other`、`candidate:*`。
- 配件 / 包装现在按 Erika 最新口径允许进入主聚合，但必须使用当前子品类 taxonomy 中明确的配件、零件、包装、到货损坏类 aspect，例如 `accessory_storage`、`missing_parts`、`packaging`、`shipping_damage`。
- 包装 / 到货损坏不是按品类特殊处理：`waders` 和 `床架` 只要当前 taxonomy 命中 `packaging` / `shipping_damage` 都可以聚合；纯物流时效、客服售后仍不进入产品问题 / 亮点。

## 状态枚举

| 状态 | 触发条件 | 处理 |
|---|---|---|
| `allowed` | `product_issue` / `product_highlight` 命中当前产品 taxonomy；或配件 / 包装片段命中当前 taxonomy 中的 `accessory_storage`、`missing_parts`、`packaging`、`shipping_damage` 等交付可优化 aspect。 | taxonomy gate 允许继续聚合；不主动把上游已拦截片段改成可聚合。 |
| `taxonomy_out_of_scope` | `aspect_key` 不在白名单、为 `other`、为 `candidate:*`；或旧产品、竞品、消费者画像、泛泛审计片段；或配件 / 包装片段被错贴成主产品本体 aspect。 | `can_aggregate=false`；库外正式 key 使用 `taxonomy_out_of_scope`，candidate 使用 `candidate_pending_review`。 |
| `taxonomy_missing` | `sub_category` 缺失、无法判断，或 taxonomy loader 未命中当前子品类。 | `can_aggregate=false`，`reject_reason=taxonomy_missing`。 |

## 有效样例

| sub_category | fragment | module | aspect_key | status |
|---|---|---|---|---|
| `waders` | `Water leaked through the seam` | `product_issue` | `seam_integrity` | `allowed` |
| `waders` | `the waders stayed dry` | `product_highlight` | `waterproof` | `allowed` |
| `waders` | `this pair has a chemical smell` | `product_issue` | `material` | `allowed` |
| `waders` | `The phone case leaked` | `accessory_or_bundle` | `accessory_storage` | `allowed` |
| `waders` | `box arrived damaged` | `logistics_support` | `shipping_damage` | `allowed` |
| `床架` | `no box damage` | `logistics_support` | `packaging` | `allowed` |

## 无效样例

| fragment | module | aspect_key | 为什么拦截 |
|---|---|---|---|
| `leaked like crazy` | `product_issue` | `water_leaks_through` | 这是旧 customer issue key，不是 `waders` taxonomy aspect key。 |
| `Great for light fly fishing` | `product_highlight` | `works_well_for_use_case` | 库外标签，需降级 candidate / other，不能直接进正式亮点。 |
| `I have not used them in water` | `audit_filter` | `other` | `other` 不得直接聚合。 |
| `living room try on` | `other_candidate` | `candidate:living_room_try_on` | candidate 未人工确认，不得直接聚合。 |
| `old AirPod 3 battery died quickly` | `comparison_or_other_product` | `battery_life` | 旧产品污染，不得映射成当前产品问题。 |
| `phone case leaked` | `accessory_or_bundle` | `waterproof` | 配件问题可以统计，但不能错贴成主产品防水问题；应使用 `accessory_storage` 等配件 aspect。 |
| `small leaks` with empty `sub_category` | `product_issue` | `waterproof` | taxonomy 缺失，fail closed。 |

备注：使用场景、人群、购买原因和期待缺口不走产品问题 / 亮点 taxonomy 白名单硬判。它们会并入 5.9.4「candidate / other + 多模块承接」：`consumer_profile` 流向消费者画像，`purchase_motive` 流向消费动机，`unmet_need` 流向未满足需求。

## 10 条人工验收表

完整 JSON 位于 `backend_api/tests/fixtures/review_fragment_taxonomy_5_9_2_samples.json`。

| # | 样例 | 来源 | 检查点 | taxonomy 结果 |
|---:|---|---|---|---|
| 1 | `row-55-cluster-plus-real-leak` | `customer_label_waders_step2_gating.json` | 当前 waders 接缝漏水 | `seam_integrity` 命中，允许聚合。 |
| 2 | `row-5-old-product-boundary` | `customer_label_waders_step2_gating.json` | 旧 waders 漏水 + 当前产品气味/尺码 | 旧产品 `waterproof` 拦截；当前 `material`、`size_fit` 命中。 |
| 3 | `row-46-phone-case` | `customer_label_waders_step2_gating.json` | phone case 配件漏水 + waders 干燥 | 配件问题以 `accessory_storage` 命中并进入主聚合；当前 waders `waterproof` 亮点命中。 |
| 4 | `row-70-fit-not-used` | `customer_label_waders_step2_gating.json` | 尺码命中、未下水、candidate | `size_fit` 命中；`other` / `candidate:*` 不聚合。 |
| 5 | `row-18-mixed-fly-fishing` | `customer_label_waders_step2_gating.json` | 库外 use-case + 抓地/舒适问题 | `works_well_for_use_case` 拦截；`grip`、`comfort` 命中。 |
| 6 | `waders-waterproofing-only` | `review_fragment_5_9_1_samples.json` | 旧 issue key 被当 aspect | `water_leaks_through` 不在 taxonomy，拦截。 |
| 7 | `row-7-duplicate-size` | `customer_label_waders_step2_gating.json` | 同评论重复 `size_fit` | 两个片段 taxonomy 均命中；第二个由 5.9.1 重复门禁不重复计数。 |
| 8 | `airpods-mixed-positive-and-negative` | `review_signal_step9_1_airpods_minimal.json` | `earbuds` taxonomy 缺失 | 所有产品问题/亮点均 `taxonomy_missing`，不聚合。 |
| 9 | `airpods-audit-filter-review` | `review_signal_step9_1_airpods_minimal.json` | 纯物流、旧产品、配件外观且 `earbuds` taxonomy 缺失 | 全部不进入当前产品聚合。 |
| 10 | `bed-frame-box-damage-packaging` | `customer_label_50u_gold_samples.json` | 包装 / box damage 反馈 | `packaging` 命中 `床架` taxonomy，允许进入主聚合。 |

## 验收结论

5.9.2 现在具备进入 Erika 人工验收的最小能力：正式产品问题 / 亮点只有命中当前 `sub_category` taxonomy 的 aspect_key 才能继续聚合；配件和包装可以作为卖家需要优化的主聚合维度；库外、candidate、other、taxonomy 缺失、旧产品 / 竞品 / 消费者画像污染仍会 fail closed。
