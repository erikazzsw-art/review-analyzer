# 5.9.8 Step 4: TIDEWE waders session121 50 条全新盲样验收

日期：2026-07-29

## 1. 结论

`REVIEW_NEEDED`。

这次样本是 Erika 提供的 50 条全新 waders 样本与结果页 `session_id=121`。本地最新规则修复后，session121 只读回放的前台 verified occurrence 口径已经通过验收：`validation.status=PASS`，无 Top / 单条明细 / evidence / cluster propagated P0。

但当前生产结果页下载文件仍不能判 PASS：

- Erika 下载的 raw review 文件仍是旧 29 列，没有 `Audit ...` 分列。
- 下载文件前台字段仍混入 audit/candidate 结果：issue `Evidence Verified=false` 16 个、issue `Cluster Propagated=true` 16 个。
- 下载文件里有 16 条 evidence 不在当前 review 原文中，典型污染 evidence 为 `they leaked straight out of the box`。
- 生产 DB 只读审计显示本 session 仍有 4 条 cache rows，严格盲测口径下需标记为 `REVIEW_NEEDED`。

因此：本地最新 5.9.8 规则可以作为下一次部署/重测候选，但 session121 的线上下载结果不能作为 5.9.8 最终 PASS。5.9.9 Customer Label System v2 不应开始实现，至少要先完成生产下载口径部署验证与一次干净复测。

## 2. 范围与边界

- 结果页：`https://www.clueai-reviewlens.com/analysis/results?product_id=TIDEWE-%E4%B8%8B%E6%B0%B4%E6%9C%8D-WD001&range=default&session_id=121`
- Erika 下载文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-AGGREGATED-251-300.xlsx`
- Codex 操作：生产 DB 只读查询、本地最新规则回放、读取 Erika 下载 XLSX、本地小范围修复、fixture/test 更新、文档更新。
- Codex 未执行：生产上传、生产删除、production reanalysis、production export API、production LLM/credit 触发。

## 3. 当前 5.9.8 验收口径

前台展示字段只允许 verified source-review occurrence：

- Top10 Customer Issue / Customer Label
- 单条评论明细 Customer Issue / Customer Label
- raw review export 的 frontstage 字段
- single-tag download 的评论集合

audit/candidate 字段可以保留，但必须明确分列，不得混入用户会理解为确定结论的字段：

- 无 evidence 的候选
- `cluster_propagated=true` 的候选
- legacy fallback / catalog fallback
- 当前 review 原文无法验证的标签
- context-only 标签，例如 `not_used_yet`

同一 canonical key 在 Top10、单条明细、raw review export、single-tag download 必须使用同一套 verified occurrence 过滤口径。

## 4. 生产只读审计

| 项目 | 结果 |
|---|---|
| session | `121` |
| job | `102` |
| rows | 50 comments / 50 processed / 50 analyzed / 50 normalized dates |
| content hash | 50 distinct hashes，无 session 内重复 |
| comment cache | 46 无缓存；3 L2；1 L1 global |
| cache samples | `Great fit`、`Well built`、`Kept me dry`、`Solid` |
| review_analyze credit | DB 已存在单条 `delta=-50`，ref `102`；Codex 未触发 |
| insight credit | DB 已存在 `delta=-6`；Codex 未触发 |
| llm_usage | 31 rows；27 non-cache，4 cache；cost `0.172813` |
| review_pool backfill | same product since session only same-batch hashes；outside_batch_rows=0 |
| Codex 只读连接 | `transaction_read_only=on`，`default_transaction_read_only=on` |

判定：样本本身是全新 50 条，但严格无缓存盲测口径仍有 4 条 cache，需要保留 `REVIEW_NEEDED` 标记。

## 5. Erika 下载文件审计

文件：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-AGGREGATED-251-300.xlsx`

| 项目 | 结果 |
|---|---|
| sheet | `Raw Reviews`, `Export Info` |
| rows | 50 |
| columns | 旧 29 列 |
| `Audit ...` 分列 | 缺失 |
| issue evidence false | 16 |
| issue cluster true | 16 |
| highlight evidence false | 0 |
| highlight cluster true | 0 |
| evidence not found | 16 |
| download vs local latest row diff | 40 |

下载文件 frontstage 计数：

| 类型 | key | count |
|---|---|---:|
| issue | `water_leaks_through` | 28 |
| issue | `runs_too_small` | 1 |
| issue | `feels_thin_and_flimsy` | 1 |
| highlight | `fits_as_expected` | 3 |
| highlight | `keeps_water_out` | 2 |
| highlight | `good_value_for_the_price` | 2 |

主要问题：raw 下载仍把 propagated / evidence false issue 放进前台字段，且同一条不在当前 review 中的漏水 evidence 被扩散到多条评论。该问题分类为 `导出口径错 / 生产版本问题`。

## 6. 本地最新规则验收

artifact：

- summary：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session121-readonly/acceptance-summary.json`
- download audit：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session121-readonly/download-file-audit.json`
- local results：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session121-readonly/session-results.json`
- local raw equivalent：`tmp/5.9.8-step4-tidewe-waders-prod-20260729/session121-readonly/frontend-raw-reviews-equivalent.xlsx`

本地验收指标：

| 项目 | 结果 |
|---|---|
| validation.status | `PASS` |
| comments | 50 |
| Top violations | 0 |
| single-detail no evidence display | 0 |
| single-detail cluster propagated display | 0 |
| evidence not found | 0 |
| water false positive | 0 |
| water false negative candidates | 0 |
| pocket false negative candidates | 0 |
| four-pack bad rows | 0 |
| negative label bad rows | 0 |

本地 frontstage Top：

| 类型 | key | mention_count | evidence 示例 |
|---|---|---:|---|
| issue | `water_leaks_through` | 12 | `Not waterproof`、`did not keep him dry`、`green ones weren't`、`leak`、`leaked` |
| issue | `size_fit_problem` | 5 | `sizing is not great`、`fit like they are a 10`、`not for a bigger guy`、`no me queda`、`difícil encontrar la talla adecuada` |
| issue | `breaks_easily` | 1 | `ripped on the leg...` |
| issue | `not_breathable` | 1 | `do NOT breath` |
| issue | `pocket_not_waterproof` | 1 | `pocket isn’t waterproof` |
| highlight | `fits_as_expected` | 9 | `fits true`、`fits true to size`、`perfect size`、`Great fit`、`fit great` |
| highlight | `good_value_for_the_price` | 9 | `for the price`、`good quality for the price`、`Good value`、`great value` |
| highlight | `keeps_water_out` | 5 | `No leaks`、`No leaks!`、`Nice and dry`、`Kept me dry`、`stayed dry` |
| highlight | `overall_satisfied` | 4 | `as described`、`Worked perfectly`、`These are great`、`highly recommend` |

## 7. 错误分类与本地修复

本轮发现并修复的本地生成层问题：

| 分类 | 样例 | 修复 |
|---|---|---|
| FP highlight | `did not keep him dry` 被误触 `keeps_water_out` | 为 `keeps_water_out` 增加负向 dry / not waterproof guard |
| FN issue | `did not keep him dry`、`green ones weren't`、`got soaked`、`Water sinks in` 漏召回 `water_leaks_through` | 增补真实当前产品漏水 pattern 与 evidence pattern |
| FP issue | `ALL WADERS LEAK EVENTUALLY`、`1 pair finally leaked` 被当作当前产品漏水 | 增加历史/泛化漏水阻断 |
| FN issue | `fit like they are a 10`、`not for a bigger guy`、`no me queda`、`talla de botas fue muy pequeña` 漏召回 `size_fit_problem` | 增补英文/西语 size-fit evidence |
| FN issue | `do NOT breath` 漏召回 `not_breathable` | 增补拼写原文 pattern |
| evidence 错 | pocket review evidence 过长，且不应转成整件 waders 漏水 | 缩短 `pocket_not_waterproof` evidence 到 `pocket isn’t waterproof`，并继续阻断 `water_leaks_through` |
| 生产口径 | 下载文件无 Audit 分列，frontstage 混入 propagated/evidence false | 本地等价导出口径 PASS；生产需部署/复测 |
| cache/生产版本 | session121 有 4 条 cache rows | Codex 未清理、未重分析；下一次干净复测需确认 |

## 8. 自动化验证

已执行：

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session121_blind_regression.py -q
```

结果：PASS，`2 passed in 1.20s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session121_blind_regression.py backend_api/tests/test_customer_label_waders_session120_human_gold.py backend_api/tests/test_customer_label_waders_step4_gold.py -q
```

结果：PASS，`12 passed in 6.47s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_waders_session121_blind_regression.py backend_api/tests/test_customer_label_waders_session120_human_gold.py backend_api/tests/test_customer_label_waders_step4_gold.py backend_api/tests/test_customer_label_waders_step2_gating.py backend_api/tests/test_specific_issue.py backend_api/tests/test_customer_label_phase6_validation.py backend_api/tests/test_export_customer_label_phase5.py backend_api/tests/test_customer_label_50u_readiness.py backend_api/tests/test_outdoor_waders_taxonomy.py -q
```

结果：PASS，`82 passed in 9.33s`

```bash
npm run typecheck
```

cwd：`frontend`

结果：PASS，`tsc --noEmit`

```bash
git diff --check
```

结果：PASS

## 9. 下一步

5.9.8 的下一步不是扩大 waders 手写规则，也不是启动 5.9.9 实现，而是完成以下收口：

1. 部署本地最新 5.9.8 规则与 raw/audit 分列导出口径。
2. Erika 再次从前端下载 session121 或重新上传一批干净 50 条，确认 raw 下载出现 `Audit ...` 分列。
3. 检查 Top10、单条明细、raw review export、single-tag download 是否全部只使用 verified source-review occurrence。
4. 若生产下载文件不再有 evidence false / cluster propagated 前台污染，且本地/线上抽查无 P0，再进入 `5.9.9 Customer Label System v2 Step 1: v2 输出契约与回退策略`。
