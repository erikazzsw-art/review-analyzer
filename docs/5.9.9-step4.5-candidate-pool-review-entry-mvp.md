# 5.9.9 Step 4.5: Candidate Pool Review Entry MVP

日期：2026-07-30

状态：`PASS`

## 1. 结论

已完成 Candidate Pool 轻量审核入口 MVP 的本地闭环：

```text
candidate pool artifact/items
  -> review action validation
  -> apply action to local reviewed artifact
  -> stable reviewed JSON + reviewed CSV export
```

本轮不替换前台展示，不写 production DB，不触发 production upload / reanalysis，不消耗 credit，也不调用真实 LLM。frontstage 仍只消费 5.9.8 verified display occurrences。

核心 artifact：

- `tmp/5.9.9-step4.5-candidate-pool-review-entry-mvp/waders-shadow-summary.json`
- `tmp/5.9.9-step4.5-candidate-pool-review-entry-mvp/candidate-pool.json`
- `tmp/5.9.9-step4.5-candidate-pool-review-entry-mvp/candidate-pool.csv`
- `tmp/5.9.9-step4.5-candidate-pool-review-entry-mvp/candidate-pool-reviewed.json`
- `tmp/5.9.9-step4.5-candidate-pool-review-entry-mvp/candidate-pool-reviewed.csv`

## 2. 实现范围

更新：

- `backend_api/app/services/customer_label_v2_candidate_pool.py`
- `backend_api/tests/test_customer_label_v2_candidate_pool.py`
- `scripts/customer_label_v2_waders_shadow_replay.py`

新增：

- `docs/5.9.9-step4.5-candidate-pool-review-entry-mvp.md`

主要能力：

- 输入 candidate pool artifact 或 candidate pool items。
- 输入 review actions，并复用 Step 4 action names。
- 对 action 做 pure validation。
- 生成 reviewed candidate pool artifact。
- reviewed item 保留 `source_candidate_item`、`action`、`validation_errors`、`review_status`、`reviewed_at`。
- invalid action 不改变 source item 的状态，只进入 `action_audit` / `validation_errors`。
- reviewed JSON/CSV 均为本地 artifact，不写 DB。

## 3. Action Behavior Contract

| Action | Reviewed status |
|---|---|
| `accept` | `accepted` |
| `reject` | `rejected` |
| `ignore` | `ignored` |
| `correct_label` | `corrected_label` |
| `correct_evidence` | `corrected_evidence` |
| `needs_new_label` | `needs_new_label` |
| invalid action | source item remains `pending`; error only |

Conditional required fields:

- `correct_label`: `label_type`, `canonical_label_key`
- `correct_evidence`: `evidence_candidate`
- `needs_new_label`: `raw_label`

## 4. Reviewed Artifact

reviewed artifact schema：

- `customer-label-v2-candidate-pool-reviewed-mvp.1`

Step 4.5 replay reviewed artifact summary：

| Metric | Value |
|---|---:|
| reviewed item count | `2` |
| action count | `2` |
| valid action count | `2` |
| invalid action count | `0` |
| audit error count | `0` |

Reviewed status distribution:

| Status | Count |
|---|---:|
| `accepted` | `1` |
| `needs_new_label` | `1` |

The reviewed artifact keeps source candidate trace:

- unknown label keeps `candidate:boot_seam_leak`, `unknown_label`, `review_ids`, `source_candidate_ids`.
- maturity blocked item keeps `water_leaks_through`, `maturity_blocked`, original `review_status=pending`.

## 5. Waders Replay

`python3 scripts/customer_label_v2_waders_shadow_replay.py`

结果：`PASS`，P0=`0`

| Dataset | Reviews | Issue TP/FP/FN | Highlight TP/FP/FN |
|---|---:|---:|---:|
| session120 human gold | 50 | `22 / 0 / 0` | `49 / 0 / 0` |
| session121 blind boundary | 11 | `11 / 0 / 0` | `2 / 0 / 0` |
| session122 postdeploy acceptance | 50 | `16 / 0 / 0` | `26 / 0 / 0` |

六个重点标签仍为 FP/FN=0：

| Label | TP | FP | FN |
|---|---:|---:|---:|
| `water_leaks_through` | 12 | 0 | 0 |
| `keeps_water_out` | 8 | 0 | 0 |
| `pocket_not_waterproof` | 1 | 0 | 0 |
| `fits_as_expected` | 11 | 0 | 0 |
| `good_value_for_the_price` | 19 | 0 | 0 |
| `holds_up_well` | 7 | 0 | 0 |

## 6. Local Verification

已执行：

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_candidate_pool.py -q
```

结果：`16 passed in 0.14s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_candidate_pool.py backend_api/tests/test_customer_label_v2_shadow.py -q
```

结果：`34 passed in 0.47s`

```bash
python3 -m pytest backend_api/tests/test_customer_label_v2_candidate_pool.py backend_api/tests/test_customer_label_v2_shadow.py backend_api/tests/test_customer_label_waders_session121_blind_regression.py backend_api/tests/test_customer_label_waders_session120_human_gold.py backend_api/tests/test_customer_label_waders_step4_gold.py backend_api/tests/test_customer_label_waders_step2_gating.py backend_api/tests/test_specific_issue.py backend_api/tests/test_customer_label_phase6_validation.py backend_api/tests/test_export_customer_label_phase5.py backend_api/tests/test_customer_label_50u_readiness.py backend_api/tests/test_outdoor_waders_taxonomy.py -q
```

结果：`116 passed in 6.56s`

```bash
python3 scripts/customer_label_v2_waders_shadow_replay.py
```

结果：`{"status": "PASS", "p0_count": 0, ...}`

```bash
python3 -m ruff check backend_api/app/services/customer_label_v2_candidate_pool.py backend_api/tests/test_customer_label_v2_candidate_pool.py scripts/customer_label_v2_waders_shadow_replay.py
```

结果：`All checks passed!`

```bash
git diff --check
```

结果：`PASS`

## 7. Safety Boundary

本轮明确未执行：

- 未 production upload。
- 未 production reanalysis。
- 未 production DB write。
- 未 review_pool/cache 清理。
- 未扣 credit。
- 未调用真实 LLM。
- 未替换或修改 frontstage 展示消费路径。

reviewed artifact safety flags：

- `production_upload=false`
- `production_write_path=false`
- `production_db_write=false`
- `db_write=false`
- `credit_consumed=false`
- `llm_called=false`
- `frontstage_replaced=false`
- `frontstage_mutated=false`
