# 5.9.8 Step 3: TIDEWE waders session 119 干净重传只读验收

日期：2026-07-29

## 结论

`REVIEW_NEEDED`。

本轮确认 session 119 已经摆脱 session 118 的用户侧 L1 / 全局 `review_pool` 已分析缓存污染：100 条评论均落入同批 content hash，`comments.cache_hit_level/cache_hit_source/cache_source_id` 全为空，`review_pool` 新回填仅限同批 49 个 content hash，`credit_ledger` 有且仅有一次 `review_analyze=-100`。

但 Step 3 仍不能 PASS：

- `water_leaks_through` 仍被 phone case / 旧产品历史漏水 / no-leaks 正向语境污染。
- `pocket_not_waterproof` 未形成 verified source-review occurrence；单标签导出为 0 行。
- 实际前端 raw review 下载的 Customer Issue / Customer Label 审计列仍包含 `cluster_propagated=true` / `evidence_verified=false` 的 propagated occurrence，和结果页 Top / 单标签下载的 frontstage 口径不一致。
- `llm_usage_log` 中存在 2 条 `cache_hit=true` 的 L2 短文本规则命中；不是用户历史或全局 `review_pool` L1 复用，但说明本轮不是“100% non-cache LLM”。同时这 2 条没有写回 `comments.cache_hit_level`，存在观测口径不一致。

## 范围与边界

- 产品：`TIDEWE-下水服-WD001`
- category：`waders`
- session：`119`
- job：`100`
- 结果页：`https://www.clueai-reviewlens.com/analysis/results?product_id=TIDEWE-%E4%B8%8B%E6%B0%B4%E6%9C%8D-WD001&range=default&session_id=119`
- Codex 操作：生产 DB 只读查询、本地等价导出、读取 Erika 提供的实际下载 XLSX、本地文档更新。
- Codex 未执行：生产上传、生产删除、生产 `review_pool` 清理、生产 reanalysis、生产 `/export/full` API、任何主动 credit / LLM 调用。

## 产物路径

- 只读验收摘要：`tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/acceptance-summary.json`
- 本地结果 JSON：`tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/session-results.json`
- 前端 raw 等价导出：`tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/frontend-raw-reviews-equivalent.xlsx`
- 后端完整导出：`tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/backend-full-export.xlsx`
- Erika 提供的实际前端 raw 下载：`/Users/zhangxi/Desktop/TIDEWE-下水服-WD001-AGGREGATED-raw-reviews -0729.xlsx`
- Erika 提供的上传源文件：`tmp/5.9.8-step3-tidewe-waders-prod-20260729/TIDEWE-下水服-WD001-step3-upload-100rows-V1.xlsx`

单标签等价导出：

- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-issue-water_leaks_through.xlsx`
- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-issue-pocket_not_waterproof.xlsx`
- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-highlight-keeps_water_out.xlsx`
- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-highlight-fits_as_expected.xlsx`
- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-highlight-good_value_for_the_price.xlsx`
- `tmp/5.9.8-step3-tidewe-waders-prod-20260729/session119-readonly/single-tag-highlight-holds_up_well.xlsx`

## 干净重传审计

| 项目 | 结果 |
| --- | --- |
| session rows | 100 comments / 100 processed / 100 analyzed / 100 normalized dates |
| content hash | 100/100 匹配本次人工样本；无 session 内重复 hash |
| comments cache fields | `cache_hit_level=NULL`, `cache_hit_source=NULL`, `cache_source_id=NULL`：100 行 |
| llm usage | 43 rows；其中 41 non-cache、2 cache；总 cost `0.324587` |
| comment-analysis LLM | `deepseek-chat` 36 non-cache rows，cost `0.237188` |
| post-result translate LLM | `gpt-4o-mini-2024-07-18` 2 rows；`deepseek-v4-flash` 3 rows |
| cache rows | 2 rows，`model_name=cache/provider=cache/cache_hit=true/cost=0`；对应短文本 `Fit great`、`See title`，`prompt_version=cache_l2` |
| review_analyze credit | 单条 ledger，delta `-100`，ref `100` |
| 其他 ledger | Erika 前端查看/下载后产生 `insight` 2 次、`translate` 3 次、`export` 1 次；非 Codex 触发 |
| review_pool backfill | 同批 49 rows / 49 distinct hashes，`analyzer_version=v4_deep`，`outside_batch_rows=0` |

判定：用户侧 L1 / 全局 `review_pool` L1 干净；但 L2 短文本规则仍产生 2 条 cache usage，且未同步写入 `comments.cache_hit_level`。

## 结果页 Top

Customer Issue Top：

| key | count | evidence | gate |
| --- | ---: | --- | --- |
| `water_leaks_through` | 12 | `leak`; `leaking`; `water enters through`; `not waterproof`; `leaked` | verified / non-propagated |
| `breaks_easily` | 1 | `already broke at the suspender` | verified / non-propagated |
| `feels_thin_and_flimsy` | 1 | `Strong chemical, plastic smell. Thin plastic`; `does not appear sturdy` | verified / non-propagated |
| `missing_wader_hanger` | 1 | `did not come with wader hanger in product description` | verified / non-propagated |
| `runs_too_small` | 1 | `The size chart lists this size up to a 10 inch shoe size.` | verified / non-propagated |

Customer Label Top：

| key | count | evidence | gate |
| --- | ---: | --- | --- |
| `holds_up_well` | 5 reviews / 7 verified occurrences | `These have held up great this season`; `Still going strong 4 years later`; `its tough` | verified / non-propagated |
| `fits_as_expected` | 3 | `Boots fit perfect...`; `got true to my shoe size...`; `this product fit pretty well` | verified / non-propagated |
| `good_value_for_the_price` | 3 | `good value for the proce point`; `good value, recommend`; `cost way less than store` | verified / non-propagated |
| `easy_to_use` | 2 | `Easy to fit into`; `easy to use` | verified / non-propagated |
| `feels_well_made` | 2 | `Good quality`; `Decent thickness`; `good quality` | verified / non-propagated |
| `keeps_water_out` | 2 | `No leaks`; `never get wet` | verified / non-propagated |
| `arrives_on_time_and_intact` | 1 | `Fast shipping` | verified / non-propagated |
| `comfortable_to_wear` | 1 | `super flexible` | verified / non-propagated |
| `looks_good` | 1 | `Looked so nice on them too. Didn’t look cheap at all.` | verified / non-propagated |
| `useful_storage_space` | 1 | `great pocket, convenient storage hanger hook and phone protector` | verified / non-propagated |

Top gate：未发现 `evidence_verified=false` 或 `cluster_propagated=true` 进入 Top。

## 单条评论明细

按 29 条人工验收重点行检查：

- 单条明细无 evidence 展示率：`0`
- 单条明细 `cluster_propagated=true` 展示率：`0`
- 四件套未在单条明细展示字段中批量扩散
- 但 `water_leaks_through` 仍有语义污染：row 46、row 52、row 62

实际前端 raw 下载中的 `Issue Tags` / `Highlight Tags` 字段可视为前端展示标签：

- `Issue Tags`：16 行有展示标签；`Water Leaks Through` 12 行，`Feels Thin and Flimsy` / `Missing Wader Hanger` / `Breaks Easily` / `Runs Too Small` 各 1 行。
- `Highlight Tags`：15 行有展示标签；四件套没有在无 evidence / 未使用 / 泛泛好评 / 负面评论中批量进入展示标签。

## 导出核对

实际前端 raw 下载：

- `Raw Reviews`：100 数据行，29 列。
- `Export Info`：4 数据行。
- 100 条评论集合与后端完整导出一致；不依赖行顺序比对，评论原文集合完全相等。
- 与后端完整导出的 canonical/evidence/verified/cluster 审计列基本一致；唯一差异是 row 25 的 issue occurrence 顺序不同。

后端完整导出：

- `总览摘要`
- `源评论分析明细`：100 数据行，29 列
- `TOP10 核心问题点`
- `TOP10 产品亮点`
- `AI Notice`

导出异常：

- 实际前端 raw 下载的 Customer Issue 审计列中有 9 行包含 `cluster_propagated=true`，10 行包含 `evidence_verified=false`。
- 实际前端 raw 下载的 Customer Label 审计列中有 44 行包含 `cluster_propagated=true`，44 行包含 `evidence_verified=false`。
- 四件套 propagated / unverified 在 raw 审计列中出现 42 行，典型行包括 100、99、95、92、91、85、88、83、81、77、73、71。
- 这说明 raw 审计列当前是“全量 audit occurrence”，不是“前台展示等价 occurrence”；若验收标准要求 raw 与页面明细一致，则该项失败。

## 单标签下载核对

| label | rows | evidence | cluster |
| --- | ---: | --- | --- |
| `water_leaks_through` | 12 | 全部非空且 `true` | 全部 `false` |
| `pocket_not_waterproof` | 0 | 无 verified occurrence | 无 |
| `keeps_water_out` | 2 | 全部非空且 `true` | 全部 `false` |
| `fits_as_expected` | 3 | 全部非空且 `true` | 全部 `false` |
| `good_value_for_the_price` | 3 | 全部非空且 `true` | 全部 `false` |
| `holds_up_well` | 7 | 全部非空且 `true` | 全部 `false` |

单标签与 Top 的 gate 口径一致：只导出 verified source-review occurrence；无 propagated / unverified / missing evidence 行。

## 重点异常样例

- row 46：phone case 漏水仍进入 `water_leaks_through`，evidence 为 `not waterproof`；应归 `pocket_not_waterproof` / `accessory_storage`。
- row 52：历史/旧 waders 漏水背景仍进入当前产品 `water_leaks_through`。
- row 62：`haven’t had any issues with leaks yet` 是正向防水语境，却进入 `water_leaks_through`。
- row 4：`pocket_not_waterproof` 只以 propagated/unverified 形式出现在 raw 审计列，evidence 还错成 `did not come with wader hanger...`；未进入 verified 单标签下载。
- row 100：负面泛评 `These waders are trash` 在 raw 审计列出现四件套 propagated/unverified：`fits_as_expected`、`holds_up_well`、`keeps_water_out`、`good_value_for_the_price`。
- row 70：未实际使用评论在 raw 审计列出现四件套 propagated/unverified。
- row 81：`runs_too_large` 在 raw 审计列使用了 boot-fit 正向 evidence，且同条 raw 审计列出现 propagated 四件套。

## 后续建议

- Step 3 修复重点应转向分析语义层：`water_leaks_through` context guard、phone case / pocket 归类、旧产品/历史漏水过滤、no leaks 正向边界。
- raw review 前端下载若定义为“页面明细等价导出”，需过滤 `isVerifiedSourceReviewOccurrence` 后再 join Customer Issue / Customer Label 审计列；若定义为“全量 audit 导出”，则需要在文件中明确区分 audit/candidate 与 display。
- 修复后再次由 Erika 手动上传新 session；Codex 仍只做只读验收。
