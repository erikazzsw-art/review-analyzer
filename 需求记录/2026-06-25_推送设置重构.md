# 推送设置页重构 + 推送内容增强

## 需求描述

### Part A: 设置页面重构

1. 设置页改为 sidebar 导航，分 3 个子页面：
   - `/settings/push` — 绑定飞书 + 推送规则（全局 + 产品级 + 周期 + 升级）
   - `/settings/api-keys` — DeepSeek API Key 管理
   - `/settings/billing` — Paddle 订阅计费
2. 推送设置页改为全宽单列布局，移除右侧栏
3. 产品级规则支持添加/编辑/删除
4. 环比窗口锁定 14 天

### Part B: 推送内容增强

- B1: 问题/亮点输出条数 + 占比（如 "36条, 23.4%"）
- B2: 附加 AI 总结和建议（来自 insight_engine.build_results_insights()）
- B3: 附加可点击链接（分析详情 + 行动中心）
- B4: 升级行动增加引导文案 + 行动中心链接
- B5: 环比推送增强（对比周期、上期→本期、TOP3 变化含条数）
- B6: TOP 问题复盘进度（来自 action_store 状态）

## 工作量评估

| 模块 | 工作量 | 说明 |
|------|--------|------|
| Part A 前端路由+Layout | S | 新建 layout.tsx + 3 个 page.tsx + redirect |
| Part A 推送设置面板 | M | 合并 settings-panel + smart-push-settings 为 push-settings-panel |
| Part A API Keys / Billing | S | 从旧面板拆分迁移 |
| Part B notifier 增强 | L | 6 个子需求，涉及 insight_engine + action_store + 环比数据结构化 |
| Part B workers 集成 | S | periodic_jobs.py + jobs.py 传参适配 |

**总体评估：L（大需求）**

## 涉及岗位及工时

| 岗位 | 预估工时 |
|------|---------|
| 前端开发 | 4h（路由重构 + 面板合并 + 子页面拆分） |
| 后端开发 | 6h（notifier 6 项增强 + workers 集成 + 环境变量） |
| 测试验证 | 1h（lint + typecheck + build + webhook 手动测试） |

## 变更文件清单

### 前端（Part A）
- `frontend/src/app/settings/page.tsx` — redirect 到 /settings/push
- `frontend/src/app/settings/layout.tsx` — sidebar 导航 layout（新建）
- `frontend/src/app/settings/push/page.tsx` — 推送设置页（新建）
- `frontend/src/app/settings/api-keys/page.tsx` — API 密钥页（新建）
- `frontend/src/app/settings/billing/page.tsx` — 订阅计费页（新建）
- `frontend/src/components/settings/push-settings-panel.tsx` — 合并推送面板（新建）
- `frontend/src/components/settings/api-keys-panel.tsx` — API Key 面板（新建）
- `frontend/src/components/settings/billing-panel.tsx` — 计费面板（新建）

### 后端（Part B）
- `review_analyzer/notifier.py` — 推送内容增强（B1-B6）
- `workers/periodic_jobs.py` — 传递 action_progress + product_id
- `workers/jobs.py` — 同上
