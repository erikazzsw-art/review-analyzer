# 业务场景与用户洞察 — 来自真实运营经验的需求输入

> 本文档整理自产品设计对话，记录用户（跨境电商运营）结合实际业务场景提出的所有关键信息和需求洞察。

---

## 一、用户背景

- 6年跨境电商运营经验，操盘过年销售额上亿的亚马逊店铺
- 熟悉亚马逊、沃尔玛、美客多平台运营
- 有 Python 基础，已独立完成 WhatsApp 评论分析系统（SQLite + DeepSeek API + 飞书推送）
- 目标：将现有项目升级为可商业化的 Web 产品

---

## 二、真实业务痛点（原话整理）

### 评论分析的现状痛点
- 每月需要处理数千条用户评论，人工逐条阅读、分类、总结，每周耗费 8-10 小时
- 差评响应慢：从发现差评到采取行动平均需要 3-5 天
- 洞察碎片化：运营、产品、研发看到的是不同的评论片段，无法形成统一结论
- 趋势盲区：无法及时发现评论中的新兴问题（如某批次质量问题）

### 竞品评论分析的实际操作方式
> "比如我做的不是自己产品的评论分析而是竞品的评论分析，我无法获取非常标准格式化的竞品评论内容，普通的运营只能通过这种简单的复制粘贴的方式。"

- 实际操作：从 Walmart / Amazon 前端页面 Listing 详情里复制评论，粘贴到 Word 文档保存
- 文件命名规范示例：`Walmart-Mainstays-BF001-源数据-0423.docx`
- 这种格式是真实业务场景的刚需，不是边缘情况

Walmart 复制粘贴后的实际格式：
```
Apr 18, 2026  Munju  Item details  Multipack quantity: 1
5 out of 5 stars review  Verified Purchase
Great Bed Frame. Great quality and value. Easiest assembly!
Helpful?(0)(0)Report

Apr 17, 2026  Cheryl  Item details  Multipack quantity: 1
3 out of 5 stars review  Verified Purchase
looks great but one of the ends is loose and no way to tighten it.
Helpful?(0)(0)Report
```

---

## 三、核心业务逻辑（用户明确提出）

### 好评 / 差评的定义
> "如果有包含评分的源数据，我们在分析好评差评的时候，差评只针对4分以下的评论内容提取，好评只针对4-5分的评论提取，如果没有评分就按照评论内容来进行分类，说好的评价内容就是好评，说不好的评价就是差评，中性或者无意义的评价内容直接过滤。这是这个项目非常核心的逻辑，希望可以最大程度的保证它的准确性。"

### TOP10 的正确理解
> "准确来说不应该叫做 TOP10 的好评和差评，这里可能理解有误，应该是核心问题点（也就是所有差评里反馈最多的问题，比如包装损坏是在这里比如最近30天的源评论数据的差评评论占比60%，是占比最高的它就排第一位，依次类推，产品亮点的 TOP10 逻辑一样。"

### 查看源评论的需求
> "补充 TOP10 表格后面需要有一个按钮，点击可以显示这10条核心问题点的所有源评论（但是这些源评论不是真正客户上传的原始表格，而是经过处理，比如情绪标记、痛点聚类这些基本处理后的 Markdown 格式的表格，具有一定的可视性。"

### 时间范围筛选
> "并且在 TOP10 好评和差评这个模块可以有时间范围的选项，比方说我想看最近30天或者是具体某个时间段的 TOP10 好评和差评。"

---

## 四、总览看板的业务需求

> "总览看板：希望按照我们之前沟通的，显示一共上传多少条评论，好评和差评的占比（这里的好评和差评的统计需要考虑进去哪些，比如只说好用，或者好，或者没有文字只有打分的这种好评）。类别分配是对实际有业务价值的核心亮点和核心问题点的呈现。"

### 环比数据需求
> "用户选时间后，总览看板显示环比数据针对同一个时间周期，比如客户上传的是最近30天的数据，如果系统里有上一个30天周期的数据可以呈现一个好评率和差评率数据的环比，总评论数的环比以及分类核心问题或者核心亮点的环比。如果没有就隐藏。"

### 类别自定义需求
> "关于 Prompt 核心逻辑里面的 category，先按照比如普遍家具家居类目的产品核心痛点和产品核心亮点来进行分类，然后最上面可以有一个自定义口可以让用户按照自己类目对现有初始化类别进行调整。"

---

## 五、数据持久化与历史记录需求

> "按照客户上传的文件日期，提示客户'已存在是覆盖还是追加'（追加的情况是，只有部分信息是重复的，部分是新的信息）。关于是否一起分析，可以提供给客户选项，1是只分析这次上传的内容，2是包括这次新上传的和以前的，根据客户选择的时间进行这段时间的评论进行分析。"

> "下次打开 app 还能看到历史数据。可以从历史数据去查看之前的评论分析结果，并且可以用户对历史结果进行自定义标题，我们也可以自动生成一个可以包含核心信息的初始标题，比如日期+产品信息（中文名称或SKU等）。使用户想找历史数据能快速找到它之前的分析结果。"

---

## 六、产品标识与版本批次需求

> "上传的文档必须要求有一个产品识别的唯一标识，如果上传的文档里没有这个信息，必须要客户自定义一个，方便后续客户在产品维度下做环比之类的后续动作。"

> "增加一个批次，应对产品升级的情况，可选 V1-V100 供客户选择，这个也是必选。应对比如这个产品 V1 的评论，和 V2 的评论进行对比，对升级功能是否达到我们预期的价值从用户反馈的角度给出建议。"

**版本对比的典型业务场景：**
- V1 TOP1差评：安装困难（占差评45%）→ 产品改版优化安装结构
- V2 TOP1差评：包装破损（占差评38%）→ 说明安装问题已改善，新问题出现
- 结论：从用户反馈角度验证产品升级是否达到预期效果

---

## 七、文件上传字段逻辑

> "深度思考后，决定还是将导入文件的必填项从单一的评论内容调整为评论内容和日期时间。"

> "用评论内容 MD5 哈希值检测重复：如果源文件除了日期和评论内容还有其他字段的话，要求是所有字段都相同的情况下去重，如果是只有日期和评论内容的话按照日期和评论内容完全一样去重。"

> "不需要客户手动选择，应该是如果上传的文档里没有评论内容会提示上传失败，原因是没有评论内容，其他情况都可以上传成功，有可选或者更多的参考字段更好。"

---

## 八、推送通知渠道

- 选择：飞书（首选）
- 触发时机：每次分析完成后自动推送
- 推送内容：产品标识、分析条数、差评率、TOP3核心问题（含占比）、高优先级问题数量

---

## 九、界面与技术偏好

- 界面语言：中英双语
- 框架：Streamlit（Web）
- AI 模型：DeepSeek API
- Prompt 定位：跨境电商专版（非通用版）
- 类目预设：多类目预设库（家具家居 / 3C电子 / 服装鞋帽 / 母婴用品 / 运动户外 / 美妆个护 / 厨房用品 / 宠物用品）
- V1.0 产品范围：单产品评论分析

---

## 十、V2 商业化功能需求

### Ask your reviews 评论问答

运营在查看 TOP 问题、亮点和版本对比后，常常会继续追问更具体的问题，例如“包装问题主要集中在哪里”“V2 是否真的解决了安装困难”“用户对价格的抱怨是不是共性问题”。因此 V2 增加评论问答入口：

- 输入自然语言问题，不要求用户理解筛选语法
- 系统只基于当前产品/当前筛选范围内的评论回答
- 回答必须带引用评论，方便运营回看证据，而不是只相信 AI 总结
- 主检索链路使用评论 embedding + pgvector 余弦相似度，提升语义召回能力
- 当 embedding 配置缺失或向量检索不可用时，仍保留文本检索摘要和引用，保证页面可用

### Pro 付费墙

商业化拦截点选择两个与价值强相关的位置：

- 多产品管理：Free 版允许分析 1 个产品，添加第二个产品时提示升级
- Ask your reviews：作为高级洞察能力，只对 Pro 用户开放

这样既保留新用户的首次体验，又把付费点放在“用户已经看到价值、准备扩大使用”的节点上。

---

## 十一、V2.5 产品档案与闭环跟进洞察（2026-06-03 新增）

### 为什么要先做产品管理

进入 V2.5 后，单纯按 `product_id` 看评论已经不够用了。真实业务里，运营和产研需要围绕“产品组”而不是一次次上传记录做判断：

- 同一个父体产品下会有多个颜色、尺码、款式和功能差异。
- 后续行动事项、复盘追踪、多产品对比都必须挂在稳定的产品档案上。
- 历史上传数据很多还只有文本 `product_id`，所以新产品管理必须兼容旧数据，不能要求用户先手动迁移才能查看。

### 本地实现边界

本轮本地开发优先解决“资产沉淀”和“旧数据兼容”：

- 新增产品组、变体、版本、行动事项、复盘追踪、对比报告的数据表结构。
- 新增独立 `product_store.py`，避免后续继续把新业务写进 `database.py`。
- 新增产品管理页，先让用户看到产品组、生命周期、当前版本、最大问题、最大亮点和变体列表。
- 如果当前数据库里还没有正式产品档案，但已有历史评论，页面也要把这些 SKU 以“历史产品/未建档”形式展示出来，保证现在项目的样子和可用性不被破坏。

### 这轮设计对后续阶段的价值

- V2.6 上传流程可以直接绑定已有产品组或变体。
- V2.7 行动中心可以把 TOP 问题挂到具体产品。
- V2.8 复盘追踪可以围绕产品组做前后对比。
- V3.0 今日工作台才能基于产品资产生成“今天该处理什么”。

### V2.5-V3.1 阅读层迁移的继续价值（2026-06-06 新增）

在迁移到 Next.js 的过程中，结果页、对比页和历史页不能只是“把旧页面翻过去”，而是要把用户最常回看的三类入口改成可以直接用 URL 打开：

- 结果页需要能按 `session_id` 直达，方便从上传完成后的跳转、历史回看或外部链接直接进入。
- 对比页需要保留标准对比和功能点定向对比，同时把“生成 AI 对比报告”变成显式动作，报告要落库，便于后续追溯。
- 历史页需要支持按批次上下文直达，这样用户可以从某个具体 session 回到它的所有历史链路，而不是只看到一个产品列表。

这一轮的本质不是视觉换皮，而是把分析阅读层从“页面状态驱动”改成“URL + 显式请求驱动”，这样后续继续迁移问评论、行动中心、复盘追踪时，整个阅读和追踪链路会更稳定。

---

## 十二、V2.6 上传流程升级洞察（2026-06-03 新增）

### 工作目的不该只是标签，而是入口

Erika 明确确认，上传前要先让用户选工作目的，而不是先填一堆字段。当前确认使用的 6 个目的为：

- 竞品调研
- 新品上线监控
- 日常评论分析
- Listing 优化
- 质量问题复盘
- 版本改版验证

这样同一批评论在进入分析前，就已经带上“我要解决什么问题”的上下文，后续页面才能更聚焦地解释结果。

### 产品绑定要贴近真实电商数据

真实业务里，一个父体产品下会有多个子变体 SKU / Child ASIN，因此上传时需要支持：

- 绑定已有产品组
- 新建产品组
- 如果原始文件里有 ASIN 或 SKU，尽量自动匹配到对应子变体
- 如果原始文件里没有可识别字段，默认只绑定到产品组
- 如果文件里混有多个变体，宁可先退回产品组层级，也不要误绑到单一子变体

这套规则的核心是“宁可保守，不误绑”，避免后续版本对比、行动事项和复盘追踪建立在错误变体上。

### 本轮本地实现价值

- 上传工作流已经从“纯手填产品信息”升级为“工作目的 + 产品绑定 + 文件解析 + 自动识别变体”。
- `sessions` 会记录 `workflow_purpose`、`product_ref_id`、`variant_ref_id`，为后续行动中心和复盘追踪提供上下文。
- 分析结果页能按工作目的提示用户下一步更应该看什么，减少“看完报告却不知道下一步”的情况。

### 上传完成后的承接必须更直接

在真实使用里，上传页不是“信息录入页”，而是用户进入洞察的入口。如果文件解析成功后，主按钮被预览表格压到下方，用户会自然觉得“我已经传完了，为什么还没看到结果”。

因此这一步的体验要满足两个要求：

- 文件解析成功后，首屏就要出现明确的下一步入口，而不是让用户滚动寻找按钮。
- 用户点击开始分析后，系统要在分析完成时直接打开结果页，并告诉用户“当前看到的就是刚分析完的这一批”。

这样做不是为了增加动画感，而是为了让上传链路符合运营的心理预期：上传完就进入分析，分析完就看到结论，中间不要有模糊等待。

---

## 十三、评论工作流与问评论重构洞察（2026-06-04 新增）

### 上传评论必须成为最明显的起点

Erika 明确希望用户一进入系统，就能知道“从哪里开始”。这说明评论分析系统的首页逻辑不该让用户先理解复杂的信息架构，而应先给出一个非常清楚的入口：

- `上传评论` 需要单独升为一级导航。
- 它要放在 `评论分析` 上方，承担“开始一轮新分析”的职责。
- 上传页本身不再承载结果阅读、对比或问答，避免用户在一个页面里被多种任务打断。

### 评论分析页要从“混合能力”改成“读报告容器”

在旧结构里，上传、结果、对比、历史、问答能力互相穿插，导致用户上传完成后不知道下一步要看哪里。新的理解是：

- `评论分析` 只是一个容器。
- 它内部只保留 `分析结果 / 对比分析 / 历史记录` 三个子页。
- 用户进入这个一级导航时，默认应直接落在 `分析结果`，而不是再做一次理解成本较高的选择。

### 结果页的阅读顺序必须符合运营的思考顺序

Erika 给出的新结构，本质上是在重新定义“看完一份评论分析报告的自然顺序”：

1. 先理解这群消费者是谁。
2. 再看产品体验里哪些反馈是正向、哪些是负向。
3. 接着判断用户为什么买。
4. 再看哪些需求没有被满足。
5. 最后才落到综合建议。
6. 原文评论放在最后作为证据层。

这说明结果页的核心不是堆功能，而是降低“从结论回到证据”的认知负担。因此每个结果模块都需要：

- 独立翻译按钮，方便运营快速读懂英文结果。
- 独立下载按钮，方便把单个模块转给不同角色。
- AI 失败时也要保留可读兜底，不能白屏。

### 对比分析要服务三类决策，而不是只做一个表

从业务上看，对比分析其实有三类典型决策：

- 同一产品不同时间段，判断问题是不是在恶化或改善。
- 同一产品不同版本，判断改版有没有解决旧问题。
- 跨产品对比，判断竞品和自家产品在消费者画像、体验、购买动机和未满足需求上的差异。

因此它需要统一成矩阵式呈现，并支持最多 5 个产品同时对比。除此之外，用户还会带着“某个功能点”的明确问题进入页面，所以功能点定向对比也必须保留，并允许“推荐标签 + 手动输入”两种方式。

### “问评论”是评论资产问答，不是通用知识库

这轮新增的 `问评论`，定位已经被 Erika 明确收敛：

- 它是基于一个或多个产品现有评论资产做问答。
- 不是新的文档上传型 RAG 知识库。
- 用户必须先选产品，不能直接对全账号评论库发问。
- 第一版最多支持 5 个产品，默认包含这些产品下的历史评论批次。

这背后的业务考虑是：运营在读完结果页或对比页后，往往会继续追问某个更具体的问题，例如“安装问题是不是集中在某个版本”“哪几个产品最常被吐槽包装”。把问答入口独立出来，能让它成为报告阅读后的自然下一步，而不是结果页里的附属小组件。

### 文案和推送设置应该留在主系统里，但不该挡住主链路

Erika 最后确认：

- `宣传文案`
- `推送设置`

都应该保留，而且提升为一级导航；但它们要放在 `复盘追踪` 下方，避免打断“上传评论 → 看结果 → 做对比/问评论 → 行动中心 → 复盘追踪”的主闭环。

这说明新的产品结构不是“删除低频能力”，而是把高频主线和低频协同页做清晰分层。

### 低频高级页为什么要迁到 Next.js

Erika 在 NX-M7 里确认，`宣传文案`、`推送设置` 和 `计费` 这类能力虽然重要，但它们不应该继续占住默认阅读路径：

- 这些功能的使用频率明显低于“上传评论 → 看结果 → 做对比 → 问评论 → 行动中心 → 复盘追踪”
- 它们更多承接商业化、协同和配置动作，适合放在独立页面里，保持主闭环足够干净
- `settings` 页要同时承接 Webhook、API Key、自动推送规则和 Paddle 升级入口，用户只需要在需要时进去一次
- `copywriter` 页是把评论洞察转成平台文案的低频高价值页，适合保留在升级后入口里，而不是塞回主阅读页

因此这轮迁移的真实目标不是“把旧页面照搬到 Next.js”，而是把低频能力从主闭环里拆出来，形成更清楚的产品分层。

---

## 十三、V2.7 行动中心落地洞察（2026-06-03 新增）

### 报告必须能变成动作

Erika 在 V2.7 的核心要求不是“再加一个页面”，而是让运营在看到 TOP 问题之后，不需要离开结果页重新整理，直接把问题转成团队事项。

因此动作入口必须尽量贴近 TOP 问题本身：

- 看到“包装破损”就能立即创建质检动作
- 看到“安装误解”就能立即创建运营动作
- 看到“功能缺失”就能立即创建产研动作
- 看到需要持续观察的问题时，可以先进入复盘跟进状态

### 为什么先做 action item，再做正式 tracker

复盘追踪最终会有独立 `review_trackers` 页面，但在 V2.7 阶段，先把“加入复盘追踪”落成一种复盘类 action item，更利于平滑迭代：

- 先把“发现问题 → 指定角色 → 保存状态”这个工作流跑顺
- 再在 V2.8 把“待复盘”的事项升级成正式 tracker
- 避免一次性把行动中心和复盘追踪同时做重，导致回滚边界不清晰

### 本轮本地实现价值

- 行动中心现在已经能承接分析结果页的 TOP 问题。
- 团队可以看到事项标题、来源产品 / 变体 / 批次、问题标签、当前占比、责任角色、建议动作和复盘时间。
- 后续 V2.8 可以直接围绕这些 action item 做“已改善 / 未改善 / 继续跟进”的正式复盘判断。

---

## 十四、V2.8 复盘追踪落地洞察（2026-06-03 新增）

### 真正的差异化不是“发现问题”，而是“证明有没有变好”

Erika 在 V2.8 的核心要求，是让系统能回答：

“我们做了改进之后，用户反馈有没有真的变好？”

所以复盘追踪不能只是把事项继续放在行动中心里，而要有独立 tracker：

- 有初始问题占比
- 有改进动作
- 有预计生效批次
- 有复盘评论范围
- 有当前占比
- 有明确结论：已改善 / 未改善 / 继续跟进 / 已完结

### 为什么要从 action item 生成 tracker

复盘追踪不是凭空开始的，它必须沿着已有行动事项继续往下走：

- 先在分析结果页发现问题

---

## 十五、V3.1 演示收口与路径体验洞察（2026-06-04 新增）

### 默认导航收敛后，必须给用户一条“可讲述的路径”

当首页和侧边栏不再铺满所有功能时，产品体验会更干净，但也会带来一个新要求：

- 用户需要知道“我现在在哪一层”
- 用户需要知道“下一步通常去哪里”
- 演示时不能靠讲解人脑补页面之间的关系

所以这轮收口的重点，不是再加一个新功能，而是把现有能力串成更自然的产品路径。

### 为什么要补统一页头和演示清单

本地实现已经有今日工作台、产品管理、行动中心、复盘追踪和全部功能，但如果缺少统一页头和典型路径提示，用户进入高级页后仍然容易迷路，尤其是：

- 对比分析在数据不足时容易只看到提示，不知道自己还在什么页面
- 宣传文案、历史记录、推送设置属于低频高级页，需要更强的“归属感”
- 演示时需要一套稳定的 5-7 条路径，快速证明系统已经形成闭环

因此本轮又补了两类体验层逻辑：

- 统一页头：显示所属路径、当前页说明、常用回跳入口
- 演示清单：把“工作台 → 上传 → 结果 → 动作 → 复盘 → 高级入口”整理成 6 条标准路线

### 这轮收口的真实价值

- 对新用户来说，更容易理解这是一个“从任务出发”的工作系统，而不是页面目录。
- 对 Erika 做本地演示来说，可以直接照着路径走，不需要临场解释每个按钮为什么会出现在这里。
- 对后续继续修 UI 来说，页头层和路径层已经固定，剩下主要是局部视觉微调，而不是信息架构重做。

### 登录前入口为什么也必须统一

如果欢迎页、登录页、试用页还是旧风格，而系统内已经进入新的 V2 语言，用户会在进入产品的第一分钟里产生割裂感：

- 看起来像从一个网站跳进了另一个网站
- 会削弱“这是一个完整系统”的信任感
- 演示时需要额外解释“这里只是旧页，还没改”

所以这轮把登录前入口也一起收口，意义不只是美观，而是保证用户在注册前、试用时和登录后感受到的是同一套产品个性。

另外，在本地迭代阶段，欢迎页经常因为浏览器还保留登录态而“看起来没有变化”。因此系统补了一个仅用于本地验收的预览入口：

- 已登录时也能从侧边栏进入“预览欢迎页”
- 看完后可以一键返回工作台
- 不需要为了看入口页反复退出登录

这类小能力虽然不属于业务主功能，但能明显提高 Erika 本地走查 UI 的效率。
- 转成运营 / 产研 / 质检 action item
- 在行动中心推进到适合复盘的时点
- 再生成正式 tracker，记录前后指标和结论

这保证了复盘不会脱离原问题来源，也能让团队回看“这条复盘是从哪个问题、哪次上传、哪次动作演变来的”。

### 本轮本地实现价值

- 系统现在已经有了独立的复盘追踪页，而不是把复盘混在行动中心里。
- 行动事项可以正式进入 tracker 生命周期。
- 新上传的评论如果命中待复盘问题，结果页会主动提醒用户，不需要团队靠记忆去判断哪批评论该拿来复盘。

---

## 十四点五、V2.9 多维对比落地洞察（2026-06-03 新增）

### 对比功能不是“多放几张表”，而是帮助运营做取舍

Erika 在 V2.9 的核心要求，不只是把多个产品数据并排列出来，而是让系统能够回答这些更接近经营决策的问题：

- 哪个产品适合继续主推
- 哪个变体问题最集中
- 哪次版本改版开始见效
- 哪个批次值得继续复盘

所以对比页必须覆盖 5 类真实场景：

- 同产品时间对比
- 同产品版本对比
- 同父体变体对比
- 多产品横向对比
- 自定义批次对比

### 为什么首版先做规则化建议

这一轮本地实现仍然优先遵守“低侵入、可回滚”的原则，因此推荐动作先使用规则化生成，而不是直接依赖额外 AI 总结：

- 最高差评率对象先标为风险对象
- 最高好评率 / 最强亮点对象先标为主推机会
- 同一对象若同时高亮点和高风险，则提醒“可继续曝光，但需先修复核心问题”
- 版本或时间对比时，优先根据差评率变化和 TOP 问题变化判断“改善 / 恶化 / 持平”

这样即使暂时不接 AI，也能先把“看对比结果 → 做动作判断”这条链路跑顺。

### 为什么后来又补了一层 AI 总结

当规则版对比已经稳定后，Erika 继续要求补上 AI 对比总结，原因不是替代规则，而是补足“经营语言”这一层：

- 规则适合稳定识别风险对象、机会对象和明显差异
- AI 更适合把这些差异翻译成“适合主推 / 暂缓投放 / 继续复盘 / 优先修哪一类问题”的自然语言判断
- 两层并存时，即使 API 暂时失败，页面仍然有规则建议可用，不会让对比页失效

因此当前设计采用“双层输出”：

- 规则建议保底
- AI 总结增强解读
- 出错时只影响 AI 层，不影响对比主结果

### 本轮本地实现价值

- 系统现在已经有独立“对比分析”页，不必再只依赖结果页里的单产品环比。
- 用户可以直接选择多个产品、多个变体、多个版本或多个上传批次做横向比较。
- 页面会输出对比表、问题差异、亮点差异、风险对象、推荐动作和 AI 对比总结，方便下一步进入行动中心或复盘追踪。

---

## 十四点六、V3.0 今日工作台落地洞察（2026-06-03 新增）

### 用户进入系统后，不该先面对功能，而该先面对“今天要做什么”

Erika 在 V3.0 的核心判断是：

如果首页仍然只是产品长列表或功能导航，用户虽然“能用”，但不一定知道第一步该做什么。

所以今日工作台的目标不是再做一个漂亮的总览页，而是让不同角色一进来就看到：

- 今天最该处理的 1-3 件事
- 当前最危险的 SKU
- 哪些事项已经进入复盘期
- 下一步该点去哪个页面继续做

### 为什么要做角色视角

同一批评论，在不同角色眼里，优先级完全不同：

- 运营先看差评处理、Listing 优化和复盘
- 产研先看功能缺口、版本改版验证和横向机会
- 质检先看质量客诉、整改动作和复盘闭环
- 管理者先看风险排序、未完结事项和改版效果

如果首页不给角色视角，用户就还是要自己从产品、行动、复盘和对比几个页面里重新拼上下文，工作流并没有真的变短。

### 本轮本地实现策略

为了保持现有项目样子、方便回滚，这一轮没有把所有深层页面重新改一遍，而是采用“首页分发 + 深页承接”的方式：

- 新增 `workspace_store.py`，只负责聚合首页所需摘要数据
- `dashboard.py` 改成今日工作台，承接角色切换、今日待办、风险 SKU、待复盘和最近上传
- 产品管理、行动中心、复盘追踪、对比分析继续保留原有独立页面，不被首页重写

这样首页负责回答“先做什么”，深层页面负责回答“怎么做完”。

### 本轮本地实现价值

- 用户登录后默认先看到今日工作台，而不是旧式仪表盘长列表。
- 不同角色现在会看到不同的 2-3 个核心动作入口，路径更短。
- 首页和深层页面的职责边界更清晰，也为后续 V3.1 的“全部功能地图”留出了自然位置。

---

## 十四点七、V3.1 全部功能地图落地洞察（2026-06-03 新增）

### 默认导航不能既像工作流，又像工具箱

Erika 在 V3.1 的核心要求，是把默认导航继续收敛。

原因很直接：

- 如果侧边栏把所有页面都平铺出来，新用户仍然会被功能名称淹没
- 但如果只保留工作台，又会让高级用户担心“原来的功能是不是没了”

所以系统需要同时满足两件事：

- 默认路径足够克制，只保留核心工作流
- 完整能力依然可被找到，并且位置清晰

### 为什么要单独做“全部功能”

“全部功能”不是为了再做一个菜单页，而是为了明确产品的信息架构层级：

- 今日工作台：回答“今天先做什么”
- 默认导航：承接高频主路径
- 全部功能：收纳低频但重要的高级入口

这意味着像对比分析、分析结果、历史记录、宣传文案、推送设置这类能力，并没有被删掉，只是不再抢占默认导航位置。

### 本轮本地实现策略

这轮实现采用了最小侵入方式：

- 只新增 `pages/features.py`
- 在 `app.py` 收敛默认侧边栏为 6 个入口
- 用功能分组卡片承接原本分散在侧边栏里的高级页面

这样如果后续觉得信息架构还要调整，也只需要改导航和功能地图，不会影响工作台、产品管理、行动中心和复盘追踪本身。

### 本轮本地实现价值

- 新用户现在更容易按“工作台 → 评论分析 / 行动中心 / 复盘追踪”这条主路径完成任务。
- 高级用户仍然能在“全部功能”页找到对比分析、历史记录、宣传文案和推送设置。
- 产品整体从“功能堆叠”进一步走向“任务导向 + 能力地图”的结构。
- 高级页在打开后仍会被侧边栏归属到“全部功能”，减少“跳进深页后不知道自己在哪一层”的体验断裂。
- 统一页头、所属路径和常用跳转后，产品在演示时更像一个完整系统，而不是几个单独页面拼接在一起。
- 登录后的主应用界面已经回调到 `clueai_v2_ui_prototype.html` 对应的柔和 V2 视觉语气，不再停留在更硬朗的旧版仪表盘风格。

## 十四点八、页内快捷跳转收敛洞察（2026-06-04 新增）

### 左侧一级导航已经足够，就不要在页面里再重复一套

当一级导航已经明确提供“今日工作台、产品管理、上传评论、评论分析、问评论、行动中心、复盘追踪、宣传文案、推送设置”后，页面顶部再放一排“回今日工作台 / 去评论分析 / 去上传评论”这类按钮，会带来三个问题：

- 用户会误以为页面内这一排按钮也是主导航的一部分，信息层级变模糊
- 同样的跳转能力在左侧和正文里重复出现，页面显得更吵、更碎
- 每个页面都自己决定下一步去哪，会削弱“统一从左侧主导航切换”的使用习惯

### 这次调整的真实目标

这次不是简单删按钮，而是明确一条交互规则：

- 跨一级页面切换，只走左侧一级导航
- 页内保留的按钮，只服务当前页面自己的核心任务
- 像 `评论分析` 里的 `分析结果 / 对比分析 / 历史记录` 子页切换，仍然保留在页面内，因为那属于同一一级导航下的局部结构，不是跨页跳转

这样做之后，页面头部的职责会更单纯：只负责说明“你现在在哪、这一页是干什么的”，不再承担额外导航压力。

---

## 十五、V2.0 及未来扩展方向（用户提及）

- API 对接店铺，自动拉取评论（无需手动上传）
- 定时分析（定期自动跑分析流程）
- 版本对比视图（V1 vs V2 差评变化可视化）
- 任务分派与跟踪（运营/产品/研发/供应链协同闭环）
- 多产品管理（V1.0 只支持单产品）
- Listing 优化建议联动
- 竞品价格跟踪
- 选品辅助
- 客服话术生成

---

---

## 十六、V2.0 原型重构需求（2026-04-27 新增）

> 以下需求来自 Erika 在原型评审后提出的完整改版方案，核心方向：从"简单展示型"升级为"以产品（SKU）为核心的完整业务流程"。

### 欢迎页（Landing Page）

- 新增欢迎页，包含产品定位、痛点分析、核心优势、收益数据、CTA 入口
- 新增试用页：无需注册即可体验单次分析，但无历史保存/导出功能
- 试用页核心位置提示"登录解锁完整功能（历史保存、环比、导出、飞书）"

### 上传用户评论页（原"上传分析"）

- 页面名称改为"上传用户评论"
- 字段"产品编码 SKU" → "产品编号（例如：SKU 或任何可识别该产品的唯一编码）"
- 字段"批次名称" → "版本号"
- "分析时间段"改为选填
- 新增文件校验提示："上传的文件必须包含评论内容和日期字段"，缺失字段时报错提示具体原因
- 上传流程改为三步：① 填写产品信息 → ② 上传文件 → ③ 分析中，完成后自动跳转分析结果页

### 仪表盘重构

- 仪表盘改为以产品（SKU）为维度的长页面，每个产品一个卡片区域
- 每个产品卡片包含：SKU + 中文名 + 平台标签 + 置顶/删除操作
- 每个产品卡片展示：4 个指标卡片、情感趋势图、评分分布图、批次记录列表、行动建议
- 产品卡片支持手动置顶、排序、删除

### 环比对比升级（双维度）

> "环比对比改为双维度：版本环比（同时间段不同版本）+ 时间环比（同版本不同时间段）"

- 版本环比：同一时间段内，V1 vs V2 的指标对比（验证产品升级效果）
- 时间环比：同一版本下，不同时间段的指标对比（追踪趋势变化）
- 无对应数据时显示提示，如"暂无 V2 版本 09 月数据，上传后自动生成时间环比"
- 仪表盘和分析结果页均展示双维度环比

### 分析结果页重构

- 移除 Tabs（概览/按主题/原文列表），改为单页纵向布局：概览指标 → 情感图表 → TOP10 问题 → TOP10 亮点
- TOP10 产品问题和 TOP10 产品亮点用表格呈现，最后一列显示"查看原文 & 下载"按钮
- 点击"查看原文 & 下载"在表格下方展开代表性原文 TOP20 面板，含下载源数据按钮
- TOP10 表格每行增加勾选框，支持勾选后推送到飞书
- 新增自定义环比对比板块：可自行选择对比维度/基准/目标，支持按占比或环比值排序，支持手动置顶
- 新增行动建议区域：基于 TOP10 问题和环比变化自动生成建议

### 行动建议的业务逻辑

> 基于 TOP10 问题自动生成可落地建议，结合环比变化给出紧急程度判断。

示例：
- "产品质量"问题占比 8% → 建议：检查供应商来料质量，增加出货前抽检
- "物流慢"问题占比 6.7% → 建议：考虑切换物流渠道或使用海外仓
- "包装损坏"从 3.2% 上升到 5.8% → 建议：立即检查包装方案
- "性价比"亮点从 11% 上升到 14% → 建议：可在 listing 中强化性价比卖点

### 宣传文案 & 选品依据页（新增独立页面）

- 从分析结果页独立出来，作为单独导航项
- 选择产品编号（必选）+ 版本号（可选）加载该产品所有分析记录
- 可全选或多选分析记录，选择输出类型：广告宣传语 / 社交媒体宣传语 / 客户理想产品描述 / 选品依据报告
- 输出格式：卖点 + 真实用户姓名 + 引用原文，350 字符内
- 支持复制单条内容、导出全部内容

---

## 十七、宣传文案板块重构需求（2026-05-06 新增）

> 以下需求来自 Erika 对宣传文案生成流程的完整改版方案，核心方向：从"固定输出类型"升级为"以投放平台为导向、符合各平台广告规范的智能文案生成"。

### 以平台为导向的文案生成流程

> "首先展示平台的选择，就是客户生成的宣传文案是想投放到哪个平台。然后按照客户选择的平台，我们按照平台的广告文案要求输出对应类型的文案。"

- 第一步选择投放平台：亚马逊站内 / 谷歌广告 / Facebook / Instagram / 沃尔玛站内
- 根据选中平台，输出该平台所有广告类型的文案，每种类型有独立字符限制
- 结合评论分析结果，产出符合平台要求的文案

### 各平台广告类型与字符限制

| 平台 | 广告类型 | 字符限制 |
|------|---------|---------|
| Amazon | SP商品推广标题 | 150字符 |
| Amazon | SD展示型广告文案 | 100字符 |
| Amazon | SB品牌推广标语 | 50字符 |
| Google Ads | 广告标题 | 30字符 |
| Google Ads | 广告描述 | 90字符 |
| Google Ads | 附加信息 | 25字符 |
| Facebook | 主要文案 | 125字符 |
| Facebook | 标题 | 40字符 |
| Facebook | 描述 | 30字符 |
| Instagram | 帖子文案 | 2200字符 |
| Instagram | 故事文案 | 125字符 |
| Instagram | Reels标题 | 100字符 |
| Walmart | 商品标题 | 75字符 |
| Walmart | 商品描述 | 150字符 |
| Walmart | 广告标语 | 80字符 |

### 风格选择与刷新机制

> "每一种类型旁边再增加一个风格供客户选择，比如客户想选择幽默风趣的、简单明了的等，可以自由切换，如果不满意可以刷新重新生成。"

- 4 种风格可选：简洁专业 / 幽默风趣 / 情感共鸣 / 数据驱动
- 风格切换即时生效，无需重新点击生成
- 每条文案支持单独刷新重新生成
- 支持全部重新生成

### 自定义功能点输入

> "客户也可以自己填写某一个或者某几个产品的功能点，我们结合客户填写的功能点和客户评论的分析结果进行宣传文案的输出。"

- 提供 textarea 输入框，用户可填写产品核心卖点（如"降噪效果好、续航12小时"）
- 系统结合评论分析结果 + 用户输入的功能点生成更精准的文案
- 此项为选填，不填则仅基于评论分析结果生成

### 文案语言展示规则

> "文案的展示建议是英文呈现，翻译中文参考。亚马逊站内、沃尔玛、Facebook 基本都是以英文为主，使用我们网站的运营伙伴是中国人，如果英文不是特别好，她需要查看中文意思。"

- 文案主体以英文展示（加粗）
- 下方附中文翻译参考（灰色小字，虚线分隔）
- 复制功能仅复制英文文案（投放用）
- 字符计数仅统计英文字符数

### 平台广告违禁词合规检测

> "每个平台对于广告文案都有自己的规定，比如哪些违禁词是不能在广告中使用的，比如 best、discount 等，输出宣传文案的时候输出符合平台规则的宣传文案。"

- 各平台配置独立的违禁词规则库
- 生成文案时实时检测合规性，标注状态（✓ 合规 / ⚠ 有风险）
- 检测到违禁词时高亮列出具体词汇并建议刷新重新生成
- 生成结果顶部展示当前平台广告政策摘要

**各平台违禁词规则要点：**
- Amazon：禁止 best, #1, guaranteed, discount, free, cheap, limited time, act now 等虚假宣传和紧迫感词汇，所有声明必须可验证
- Google Ads：禁止 click here, buy now, free, guaranteed, lowest price, urgent 等误导性和点击诱导词，标题不可用感叹号
- Facebook/Instagram（Meta）：禁止 you are, your body, weight loss, beautiful, cure 等人身属性和健康声明词，避免情感操控
- Walmart：禁止 best, #1, guaranteed, discount, free, cheap 等（与 Amazon 类似），产品声明必须与 listing 一致

### 客户理想产品描述板块

> "客户理想产品描述板块不变。"

- 保持原有逻辑和展示方式不变

### 导航结构调整

- 导航顺序：仪表盘 → 上传用户评论 → 分析结果 → 宣传文案 → 历史记录 → 推送设置

### 历史记录页调整

- 历史记录按产品编号（SKU）分类展示

### 推送设置页（新增）

- 飞书 Webhook 配置
- 全局推送规则：问题占比阈值（某问题在两个批次中占比 ≥ X% 自动推送）、负面率阈值
- 产品级覆盖规则：可为单个 SKU 设置不同阈值，未设置的使用全局规则

### 飞书推送机制

- 手动推送：分析结果页勾选 TOP10 问题/亮点 → 点击推送 → 选择接收群 → 发送
- 自动推送：设置页配置规则 → 每次新批次分析完成后自动检查 → 触发推送
- 推送内容格式：产品 SKU + 批次 + 触发原因 + 具体数据

---

## 十三、推送设置页重构需求（2026-05-06 新增）

> 以下需求来自 Erika 对推送设置页面的完整改版方案，核心方向：环比逻辑细化、亮点监控新增、推送内容信息密度提升。

### 全局推送规则重构

> "全局推送规则里面的'当负面率环比上升'这里的取数逻辑是什么？建议区分的细致一些，可选 by 30天、by 60天、by 14天、by 180天，或者自定义时间环比。需要选择版本号，是相同版本不同时间环比还是不同版本相同时间都可以进行环比。"

- 环比时间窗口可选：最近14天 / 30天 / 60天 / 180天 / 自定义时间段
- 版本维度可选：同版本不同时间段（追踪趋势变化）/ 不同版本相同时间段（验证产品升级效果）
- 自定义时间段时，若源数据缺失或不足，报错提示"该时间段数据不足，请调整环比时间范围"
- 全局规则分区展示：⚠️ 问题监控 / 📊 环比监控 / ✅ 亮点监控 / 📨 其他
- 新增产品问题环比监控规则（当某个产品问题环比上升 ≥ X% 时推送）

### 产品亮点推送

> "新增一个产品亮点环比和占比阈值，产品某个亮点得到用户的多次高频赞赏也需要推送给对应的飞书伙伴或者对应的产品群，用于后期的产品研发等做参考。"

- 新增亮点占比阈值：当某个产品亮点占比 ≥ X% 时推送（推送内容含亮点名称、占比及代表性评论）
- 新增亮点环比监控：当某个产品亮点环比上升 ≥ X% 时推送（含独立的环比时间窗口和版本选择）
- 亮点推送目标：对应的产品群 / 产品研发群，用于后期产品规划参考

### 产品级自定义规则增强

> "产品级自定义规则同样增加和上面逻辑一样的环比规则。问题占比阈值和负面率阈值是或者的关系，二者只要有一个达到设定的值就进行推送。"

- 产品级规则表格新增列：亮点占比阈值、环比设置（时间窗口 + 版本维度）
- 问题占比阈值与负面率阈值为"或"关系，任一触发即推送
- 与全局规则逻辑一致，产品级可覆盖全局设置

### 推送内容信息密度提升

> "关于问题占比阈值的设置，在推送时显示具体是哪个产品问题或者产品亮点环比增加了展示给客户。推送过去的内容尽量展示该产品问题的核心主要信息，以及查看来源链接。"

- 问题告警推送内容：具体问题名称 + 占比 + 环比变化 + 时间窗口和版本信息 + 查看来源链接
- 亮点通知推送内容：亮点名称 + 占比 + 代表性评论引用 + 改进建议 + 查看来源链接
- 推送设置页增加预览区，展示问题告警（红色）和亮点通知（绿色）的飞书消息示例

---

## 十四、V1.0 测试反馈与优化（2026-05-07 ~ 2026-05-11）

> 以下内容来自 V1.0 上线后真实测试过程中发现的问题及优化方案，反映了从开发到部署再到实际使用的完整反馈闭环。

### 部署环境适配

- Streamlit Cloud 部署分支配置错误（develop vs main），导致修复推送后线上未更新 → 统一部署分支为 develop
- Python 3.9 运行环境不支持 `X | Y` 联合类型语法 → 所有模块添加 `from __future__ import annotations`
- `requirements.txt` 在子目录下，Streamlit Cloud 无法识别 → 复制到仓库根目录

### 数据准确性问题

> 运营对数据准确性要求极高，负面率偏差直接影响业务决策。

- 负面率计算偏差（25.9% vs 预期 21.93%）：有评分的评论未按评分覆写 AI 情感分析结果 → 有评分：≤3 负面、≥4 正面（覆写 AI 结果）；无评分：使用 AI 情感分析
- 无效评论（unrecognizable）被计入分母导致比例失真 → 排除 unrecognizable，页面显示「无效评论 X 条（不参与统计）」
- 情感分布饼图同样需排除 unrecognizable，改用 valid_total 作为基数
- 评分覆写遇到浮点格式（如 `'3.0'`）报错 → 转换改为 `int(float(rating))`

### 重复检测逻辑迭代

> 去重逻辑经历了三轮迭代，反映了真实数据的复杂性。

1. **初始版本**：仅用 content 字段 MD5 → 同内容不同评分/日期被误判重复
2. **第一次修正**：改为 content+rating+date+reviewer 四字段 hash → 仍遗漏 source 和其他字段
3. **最终方案**：hash 加入 source+raw_data，所有字段完全一致才判定为重复；空内容有评分不算重复；完全空行直接跳过

### 用户入口与导航优化

- 欢迎页只有"免费试用"和"登录"两个按钮，新用户找不到注册入口 → 改为三按钮布局：「免费注册」「先试用」「已有账号，登录」
- 登录页新增注册 Tab，注册按钮跳转登录页时默认选中注册

### 分析结果页功能增强

- 历史记录作为独立页面，切换成本高 → 整合到分析结果页底部，去掉 expander 直接展示
- 时间筛选只查当前 session → 支持跨 session 合并，选 90 天时自动拉取同产品同版本历史数据
- 时间环比仅有占位文字 → 完整实现：支持时间粒度（周/双周/月）+ 版本筛选（同版本/跨版本）+ 跨 session 数据合并
- 历史记录产品多时无搜索功能 → 顶部新增 SKU 搜索框即时筛选

### UI 细节打磨

- 行动建议用 HTML 标签+符号展示，普通运营看不懂 → 改为自然语言输出
- 宣传文案页平台按钮选中状态闪回 → 选中用 `type="primary"`，未选中用 `type="secondary"`
- 宣传文案页只输出占位文本 → 接入 DeepSeek API 实际生成广告文案和理想产品描述
- 小标题旁无用的锚点链接按钮 → 全局 CSS 隐藏
- 重复上传提示"没有可分析的数据"语义不清 → 区分全部重复/部分重复，提示改为"上传数据重复"

### 关键洞察

1. **数据准确性 > 功能丰富度**：运营人员对负面率、占比等核心指标的精确度非常敏感，1-2% 的偏差就会被发现并质疑
2. **去重逻辑需要考虑多种真实场景**：同内容不同评分、空内容有评分、完全空行等边界情况在真实数据中都会出现
3. **用户入口必须显而易见**：新用户首次访问时，注册/登录/试用三个路径需要一目了然
4. **跨 session 数据合并是核心需求**：用户期望按时间范围自由筛选分析所有历史数据，而非局限于单次上传

---

## 十五、规则引擎补全与 AI 质量优化（2026-05-25）

### 预警规则引擎 UI-逻辑 gap 修复

> Settings 页面早已配置了环比规则的 UI 开关（负面率环比、问题占比环比、亮点环比），但 notifier.py 的 check_global_rules() 从未实现这些逻辑，用户配置后永远不触发。

- **业务背景**：运营需要在新批次分析完成后，自动检测本次负面率/核心问题是否相比上一批次明显恶化，而不是每次手动对比
- **修复方案**：补全三条环比规则，基于同产品历史 session 自动取上期数据对比
  - 负面率环比突增：本期负面率较上期上升 ≥ N 个百分点时触发推送
  - 问题占比环比突增：某 issue_tag 在差评中占比较上期增加 ≥ N% 时触发推送
  - 亮点环比变化：某 highlight_tag 在好评中占比较上期增加 ≥ N% 时触发推送
- **取数逻辑**：查同产品（product_id）按时间倒序排列的历史 session，排除当前 session，取最近一个在时间窗口内的 session 作为「上期」
- **核心洞察**：规则引擎的价值不在绝对阈值（"负面率 > 25%"），而在相对变化（"本期比上期高了多少"）——后者才能发现批次质量突变

### AI 标签归一化

- **问题**：AI 对同一产品问题的 tag 措辞不稳定（"packaging damage" / "包装损坏" / "包装破损" 被算三个不同问题），导致 TOP10 统计被碎片化稀释
- **解决方案**：config.py 新增 TAG_NORMALIZE_MAP 标准词库，analyzer.py 写库前统一归一；新词原样保留不丢信号
- **核心洞察**：TOP10 的可靠性直接影响飞书推送告警的准确性，标签碎片化会导致单个真实问题占比被低估，可能错过告警阈值

---

---

## 十六、AI 分析质量深度优化（2026-05-25 第二批）

> 以下需求来自 Erika 对 AI 分析结果准确性的深度复盘，核心方向：从"单一情感标签"升级为"评分与内容双视角"，确保混合评价和矛盾评论不丢失信息。

### 矛盾评论处理（评分低但内容正面）

> "2星评分但内容正面时，issue_tag 被强制置空，导致差评池中该条评论无任何标签贡献。"

- **问题根因**：原逻辑认为情感=负面时才应有 issue_tag，正面内容则清空 → 丢失了真实用户的亮点反馈
- **修复方案**：`sentiment` 按评分决定（评分覆写），但 `highlight_tags` 和 `issue_tags` 均按评论**内容**实际提取，不因评分强制置空
- **业务价值**：2星差评中隐藏的正面亮点（如"发货很快"）也能进入亮点统计，更完整地反映产品实际表现

### 混合评论双标签并存

> "既有亮点又有问题的评论，issue_tag 和 highlight_tag 互斥，丢失一半信息。"

- **问题根因**：原设计将二者视为互斥字段，混合评论只能保留其中一个方向的标签
- **修复方案**：`issue_tags` 和 `highlight_tags` 改为数组格式，允许同时填写 1-3 个；新增"混合评价"分类
- **业务价值**：运营可以同时看到同一条评论中的优缺点，避免因"选边站"导致统计偏差

### 同义抱怨去重计数

> "同一评论中'包装破损''盒子压扁''运输损坏'被分别计入 3 次，实际是同一问题的不同表述。"

- **修复方案**：单条评论内引入 `seen_in_comment` 去重集合，同义标签在合并归一化后，在同一条评论中只计 1 次
- **影响范围**：analyzer.py、exporter.py、notifier.py、results.py 四处统计逻辑均同步修复
- **业务价值**：TOP10 占比更真实，不会因 LLM 输出措辞变化而人为放大某个问题的权重

### 双版本正负率对比

> "正负率只有评分版，无法反映评论内容的真实口碑——有时候评分高但大家都在吐槽某个功能。"

- **新增字段**：`content_sentiment`（纯基于评论文字内容判断情感，不受评分影响）
- **展示逻辑**：仅当同时存在评分数据和文字内容时，展示「评分版正负率 vs 内容版正负率」双列对比卡片
- **业务价值**：发现"评分虚高"或"评分虚低"的产品 — 内容负面率远高于评分负面率，说明用户嘴上给面子但实际不满意

### Prompt 版本管理机制

> "改 Prompt 后历史数据口径失控，无法追踪哪批数据用的哪个版本，环比时新旧口径混在一起会失真。"

- **技术方案**：`analyzer.py` 新增 `PROMPT_VERSION` 常量；`sessions` 表新增 `prompt_version` 列；分析结果页标题展示当前版本号
- **环比保护**：当两批对比数据的 Prompt 版本不一致时，自动显示橙色警告，提示运营注意口径差异
- **核心洞察**：版本管理不是技术洁癖，而是保证环比分析可信度的业务需求——口径不同的两批数据直接环比，结论可能完全误导决策

---

### 仪表盘多产品交互优化

> "产品多了之后，想快速看某个产品的详细分析，不想每次都手动去分析结果页选产品。"

- **产品名可点击跳转**：仪表盘中产品名称直接作为入口，点击后跳转到分析结果页并自动选中该产品，展示全部批次聚合数据
- **置顶 toggle**：支持置顶/取消置顶产品（☆ ↔ 📌），多产品时常用产品排在最前
- **删除二次确认**：删除产品需弹出确认提示，防止误操作丢失数据
- **业务价值**：多产品管理场景下减少操作路径，从仪表盘到详情分析只需一次点击

---

*整理时间：2026-04-24*
*最后更新：2026-05-26（追加仪表盘多产品交互优化：点击跳转、置顶 toggle、删除确认）*
*来源：产品设计对话，用户真实业务场景输入*

---

### 版本迭代对比分析

> "上线床架V2（新增折叠床腿）后，我想知道这个升级有没有解决用户的问题，好评率有没有提升，原来的差评问题有没有减少。"

- **版本升级说明**：运营在上传数据时填写本次版本的升级内容（如"新增折叠床腿"），系统保存并在对比视图中展示
- **统一对比输出**：同版本时间环比和跨版本对比使用相同的输出结构——核心指标（好评率/差评率/平均评分）、产品问题TOP变化、产品亮点TOP变化、行动建议
- **时间维度**：同版本看当期 vs 上期时间窗口；跨版本看各版本最近 N 天，保证数据量可比
- **核心洞察**：版本对比的价值不只是"V2比V1好不好"，而是"升级的那个点，用户感知到了吗"——通过 issue_tag 和 highlight_tag 的占比变化，可以直接看到升级功能相关问题是否减少、亮点是否被更多用户提及

*最后更新：2026-05-27（追加版本迭代对比分析场景）*

---

## 十七、ClueAI V2.5：SKU 口碑改版追踪闭环（2026-06-03）

> Erika 明确提出：ClueAI 要打造成面向中国跨境卖家的 SKU 口碑改版追踪工具，帮助运营把评论、差评、竞品反馈转化为产品改版、Listing 优化和团队行动。

### 核心定位升级

- 从“评论分析工具”升级为“产品管理 + 评论分析 + 行动跟进 + 复盘验证”。
- 用户进入系统后不应看到大量功能，而应按角色和工作目标完成任务。
- 每个高频问题都应尽量形成闭环：发现问题 → 生成动作 → 负责人处理 → 后续评论复盘 → 完结或继续跟进。

### 新增业务场景

- **竞品调研期**：产研上传竞品评论，识别必备功能、加分功能、无效功能、竞品优势、竞品短板和产品机会点。
- **新品立项期**：运营和产研基于竞品反馈与我方新增功能，辅助判断定价、发货量、销量目标和排名目标。
- **新品上线期**：运营监控首批评论，识别质量问题、使用场景误解、Listing 误导和新增功能是否被感知。
- **质量跟进期**：质检关注包装破损、配件缺失、结构松动等问题，记录改进动作并设置预计生效批次。
- **Listing 优化期**：针对用户误用场景，如墙上衣架被安装到木墙后掉落，系统提醒运营修改适用/不适用场景说明。
- **成熟竞争期**：当竞品大量涌现时，系统监控竞品新增功能、高频好评、低价优势和我方卖点流失风险。
- **复盘验证期**：对包装加固、新增灯带、改版结构等动作持续追踪，判断评论指标是否改善。

### 产品结构新增要求

- 产品管理需要支持“父体产品 + 多变体 SKU”，适配电商平台一个主体商品下挂颜色、尺码、款式、功能版本等真实结构。
- 系统需要保留“全部功能”入口，但默认入口应是今日工作台和角色任务，避免用户被功能列表淹没。
- 多次进入系统的运营，应能看到自己上传过的所有产品、评论批次、版本记录、核心问题、核心亮点和复盘结果。
- 支持同产品时间对比、同产品版本对比、同父体变体对比、多产品横向对比和自定义对比，并输出可落地建议。

### 核心判断

ClueAI 的长期差异化不是“AI 总结评论”，而是“跨境 SKU 口碑改版闭环”：让运营、产研、质检和管理者都能用少量操作完成自己的真实工作，并持续验证改进是否有效。

*最后更新：2026-06-04（追加上传后结果承接体验优化）*

---

## 十八、英文界面可用性洞察（2026-06-04）

### 英文版不能只翻左侧导航

Erika 明确确认，这次要的不是“英文导航”，而是“真正可用的英文界面”。

这意味着：

- 用户切到英文后，不能出现导航是英文、正文和表单还是中文的割裂体验。
- 一级导航命名必须自然，符合跨境团队真实工作语境。
- 页面里的按钮、步骤条、空态、筛选器、状态词和入口说明都要跟随语言切换。

所以英文版在 ClueAI 里不是装饰层，而是产品可用性的一部分。

### 一级导航命名要服务真实协作语境

本轮最终统一的一级导航英文命名为：

- `Today's Workspace`
- `Product Management`
- `Upload Reviews`
- `Review Analysis`
- `Review Q&A`
- `Action Center`
- `Follow-up Tracking`
- `Marketing Copy`
- `Notification Settings`

这背后的核心判断是：像 `Ask Reviews`、`Copywriter`、`Settings` 这样的直译或泛化命名虽然简短，但对真实用户不够自然，也不足以准确传达页面职责。

### 真正可用的英文界面，价值在于降低协作摩擦

对跨境业务来说，英文版的价值不只是“更国际化”，而是：

- 方便英文语境下的成员、海外同事或外部合作方直接理解页面内容。
- 方便把结果页、对比页、行动项和宣传文案页面直接用于英文场景下的讨论。
- 让系统在试用、演示、销售和交付阶段更像一个完整产品，而不是中文内部工具。

### 实现层面的启发

这次双语实现也反向验证了一个产品结论：

- 公共词汇、导航名、状态名、角色名必须统一收口，不能散落在各页硬编码。
- 页面应该优先保证“英文模式下完整可用”，再继续做更深的国际化抽象。
- 对工作目的、角色、状态、结果模块名这类重复值，必须用映射 helper 处理，才能保证后续扩展稳定。

这代表 ClueAI 已经从“中文优先的本地分析工具”，向“可对外展示和实际使用的双语产品界面”迈进了一步。

---

## 十九、欢迎页转化表达洞察（2026-06-04）

### 欢迎页的首要任务不是讲全功能，而是先讲清价值

Erika 明确确认，这次欢迎页不希望继续走“把功能都铺出来”的路线，而是希望更简单、更高级、更像成熟 SaaS 官网。

这意味着首页的判断标准要变化：

- 不是“功能写得全不全”
- 而是“用户 3 秒内能不能明白这是什么产品”
- 以及“用户 20 秒内能不能建立注册或试用意愿”

对 ClueAI 来说，欢迎页最值得优先表达的不是 AI 技术名词，而是：

- 上传评论后，可以更快发现高风险问题
- 可以把问题直接变成团队动作
- 可以用后续评论验证改版是否有效

### 欢迎页应该先卖“决策价值”，再卖“分析能力”

ClueAI 的长期差异化不只是“AI 会总结评论”，而是把评论洞察、责任归因和复盘验证串成闭环。

因此欢迎页的主线不应是：

- 我们支持什么分析模块
- 我们有多少功能入口
- 我们技术实现有多复杂

而应该是：

- 我今天先该处理什么
- 为什么这个问题值得优先处理
- 处理后怎么判断是否真的改善

这也是为什么新版欢迎页要从“功能导览型结构”改成“价值主张 + 真实截图 + 三个核心收益 + 差异化说明 + CTA”的原因。

### 审美方向要和系统内页一致，但结构需要更克制

Erika 确认欢迎页色调应与当前系统保持一致，不另起一套新视觉。

所以这一轮欢迎页本地预览采用的原则是：

- 沿用当前 V2 的暖白、浅粉、浅紫柔和渐变背景
- 沿用现有品牌点缀色 `#f36f8f` 和 `#8d7be8`
- 不改字体体系，不做完全脱离当前产品的新品牌包装
- 主要调整留白、层级、卡片数量和截图主次关系

换句话说，欢迎页升级不是“换风格”，而是“把现在这套风格用得更高级、更有官网转化感”。

---

## 十六、Next.js 迁移执行边界洞察（2026-06-05 新增）

### 为什么现在需要把迁移方案写成模块计划

当前项目已经不是一个可以随意整体推倒重写的小原型，而是一套仍在持续迭代的业务系统：

- 已经有工作台、产品管理、上传、分析、对比、问评论、行动中心、复盘追踪、文案、推送设置
- 已经有真实的产品资产结构和付费墙能力
- 已经形成了“评论洞察 -> 动作 -> 复盘”的主闭环

这意味着如果要迁到 Next.js，最危险的不是技术选型本身，而是：

- 一次动太多文件
- 没有清晰回滚边界
- 新旧链路同时半工作状态
- 前端迁移过程中把当前主流程改坏

所以这次迁移不能按“整站翻新”来做，而必须按“一个模块一个模块替换”来做。

### 为什么要保留 Python，而不是同时重写业务层

Erika 当前最重要的不是换语言，而是把已经验证过的业务能力搬到更适合商业 SaaS 的前端架构里。

因此迁移时默认保留：

- 现有认证能力
- 文件解析能力
- AI 评论分析能力
- RAG 检索与回答能力
- 行动事项与复盘逻辑
- 通知、导出、计费等业务能力

Next.js 负责的是：

- 前端体验
- URL 路由
- 页面状态管理
- 登录前官网表达
- 登录后应用壳层

也就是说，这次迁移的本质不是“重做系统”，而是“把已经跑通的业务能力 API 化，再接到新的前端壳层里”。

### 为什么必须要求单模块可回滚

ClueAI 当前最大的资产不是某一页 UI，而是已经形成的业务链路和本地可运行状态。

因此迁移期间每个模块都要满足：

- 影响文件范围清楚
- 启动命令独立
- 验收标准独立
- 回滚后不伤主流程

例如：

- 前端工程骨架失败，不应影响 Streamlit 主应用继续可用
- FastAPI 认证层失败，不应影响现有 Streamlit 登录
- 上传异步化失败，不应导致现有上传分析流程不可用

这套边界管理不是额外负担，而是保证后续迁移能持续推进的基础。

### 当前迁移共识

这一轮已经不再采用“先只做营销站，再长期观察产品层是否迁移”的保守口径，而是明确采用：

- 全系统逐步迁到 Next.js
- Python 业务逻辑保留
- 双栈并行过渡
- 先替换前端体验，再逐步下线 Streamlit

但执行上仍坚持：

- 一次只做一个独立模块
- 单模块通过验收后再继续
- 单模块出问题可以直接回滚

这样可以同时兼顾：

- 商业化方向明确
- 当前系统可持续迭代
- 迁移过程不破坏现有主体代码

### 为什么认证层要先从 `st.session_state` 中剥离

从当前项目真实结构看，Streamlit 版登录态完全依赖：

- `user_id`
- `username`
- `is_logged_in`
- `show_page`

这些都挂在 `st.session_state` 上。

这在 Streamlit 单体应用里可以工作，但迁到 Next.js 后会立刻变成结构性问题：

- 页面刷新后，前端无法通过 URL 或 Cookie 稳定恢复登录态
- `/me` 这种基础接口不存在，前端无法判断当前用户是谁
- 登录前后入口无法共享一套 Web 应用式会话机制

因此 `NX-M2` 先做 FastAPI 认证层，不是为了“提前重构后端”，而是为了给整个 Next.js 迁移打掉最大的结构阻塞点：

- 注册、登录、退出
- 当前用户接口
- HttpOnly Cookie 会话
- 密码重置接口

一旦这层成立，后面的工作台、产品管理、上传、分析、行动和复盘页面，才有统一的 Web 应用会话基础可以接上。

### NX-M6 闭环迁移的产品价值（2026-06-06 新增）

这一轮把“问评论 / 行动中心 / 复盘追踪”从 Streamlit 闭环迁到 Next.js 后，核心价值不只是页面换到新栈，而是闭环动作变得更明确：

- `问评论` 不再只是一个泛化问答页，而是先选 1-5 个产品，再围绕这些产品的评论范围提问，回答保留引用评论，证据链更完整。
- `行动中心` 不再只是状态看板，而是可以从结果页直接把 TOP 问题创建成团队事项，明确责任角色、建议动作和预计复盘时间。
- `复盘追踪` 不再只是把事项存下来，而是把改进前后的占比、结论状态和复盘结论持续回写，真正判断动作是否有效。

这条链路的意义在于把“发现问题”变成“分配问题、验证问题、关闭问题”，让评论分析系统更接近一个可运营的口碑追踪工作台，而不是单次分析工具。

### NX-M8 部署分层与下线边界洞察（2026-06-06 新增）

当 Next.js + FastAPI + Redis/RQ 的部署骨架确定后，真正需要被明确的不是“怎么把服务跑起来”，而是“每一层分别负责什么，以及什么时候可以下线旧入口”。

现在这套分层的职责边界已经比较清晰：

- `clueai.com` 负责品牌表达、SEO、定价、案例和转化入口，承担获客与解释产品价值的职责。
- `app.clueai.com` 负责登录后的工作台、产品管理、上传、分析、问评论、行动中心和复盘追踪，承担日常使用和核心业务闭环。
- `api.clueai.com` 负责认证、数据接口、Webhook 和后端业务能力，承担稳定性、扩展性和集成能力。

这样拆开的好处不是“看起来更像正规 SaaS”，而是可以把风险隔离开：

- 主站出问题时，不影响已登录用户继续处理业务。
- 应用站升级时，不需要跟着主站一起发布文案或营销内容。
- API 层的错误可以独立定位，不会和前端路由、页面状态混在一起排查。

Streamlit 继续保留回退口，也不是保守，而是为了在迁移期保住一条稳定的业务底线：

- 新栈出问题时，核心分析能力仍然可用。
- 迁移可以按模块推进，不需要等整站完全重做后才敢切流量。
- 用户数据、产品档案和历史评论不会因为一次发布失去入口。

对商业化来说，这种分层还有两个直接价值：

- 更容易单独优化官网转化和登录后留存，不会把两个目标混在同一个应用壳里。
- 更容易逐步下线 Streamlit 的旧入口，因为每一层都已经有对应的新承接面，而不是一次性硬切。

所以 NX-M8 的核心不是“再加一套部署文件”，而是把系统从“单体页面应用”推进到“可运营、可扩展、可逐步替换的产品架构”。

### Next.js 营销站 SEO 上线边界洞察（2026-06-06 新增）

营销站真正可上线，不只是首页能打开，而是搜索引擎能稳定识别哪些页面该收录、哪些页面不该收录。

这次补齐的 SEO 基础，核心是把页面分成两类：

- `clueai.com` 上的营销页面负责被索引，承担品牌、功能、价格和试用转化。
- `app.clueai.com` 上的登录后应用页面全部标记为 `noindex`，避免把内部工作台、分析页和设置页暴露给搜索引擎。

这样做的原因很直接：

- 搜索引擎应该找到的是“介绍产品、解释价值、引导注册”的页面，而不是登录后工作台。
- 站点地图只保留营销页，可以让收录路径更稳定，也更容易继续往博客、案例和功能页扩展。
- `robots.txt` 把内部路径挡在索引外，减少将来 SEO 资产和应用路由互相污染的风险。

这一步的意义不在于“SEO 名词补齐”，而在于让 `clueai.com` 真正具备作为独立 SaaS 主站的基础条件。后续只要继续补博客、案例和功能长页，就可以开始堆内容资产，而不会把登录后产品页也一起暴露出去。
